# 《AI Infra》大纲 v10

> **v10 改动**:§0.1 读者画像扩为两类 —— 增加"方案与选型方"(FAE、解决方案架构师、售前、技术评估),第 4、12、31 章本就是他们的日常工具;§0.7 路径表加两条选型方路径;新增 §0.9《角色与协作边界索引》,把散落各章的职责划分串成一条线。**不新增职业发展类内容**,总数仍为 31 章。
>
> **v9 改动**:§4.2 新增训练/推理负载硬件诉求差异,不采用"训练卡/推理卡"二分法;范式表加生态成熟度列;新增 §12.7 生态作为选型变量。
>
> **v8 改动**:新增第 3 章《人工智能与大模型基础》,算法内容进正文;§0.8 改写为"回答到什么程度";补齐采样后处理、Chunked Prefill、评测工具链、数据飞轮、昇腾融合五处缺口;第 23 章按加速特性三分法重构。
>
> **v7 改动**:新增 §0.7 阅读路径表与 §0.8 边界声明;第 19 章新增"微调即服务"一节。
>
> **v6 改动**:§4.1 扩为"四个数字与物理约束"(HBM、规模层级);超节点作为新层级立起并插入各章;散热落到 §31.2。
>
> **v5 改动**:硬件按架构范式组织;昇腾双轨处理 + 新增异构迁移专章;第七部分合并为 2 章。

---

## 零、写作前置约定

### 0.1 读者画像 【v10 扩充】

**主读者:平台建设方**
自建、自运维 AI 基础设施的工程师,有分布式系统或后端基础设施背景,正在接手 AI 平台建设。**不假定读者做过算法** —— 第 3 章负责补齐这一层。

**第二类读者:方案与选型方**
芯片厂商 FAE、云厂商解决方案架构师、系统集成商售前、采购侧技术评估人员。他们不一定动手实施,但必须能判断一个方案是否成立,并与客户的技术团队平等对话。

本书对这类读者的价值集中在四处:§4.2.2(负载画像与硬件配比)、第 12 章(迁移与选型决策)、§12.7(生态评估的量化方法)、第 31 章(容量与 TCO 建模)。这几节在写作时应意识到"读者可能是拿它去和客户对话的",因此**判断依据必须能独立成立,不能依赖读者手上有集群**。

> **本书不写职业发展内容**:岗位职责、能力模型、晋升路径与技术主体是两个文体,且半衰期比技术更短。读者的角色差异只体现在阅读路径(§0.7)与协作边界(§0.9)上。

### 0.2 前置知识声明(写进前言)

- **假定读者已掌握**:Linux、容器与 K8s 基础、分布式系统基本概念、至少一门系统级语言
- **本书不假定读者掌握**:深度学习理论、模型训练经验、CUDA 编程
- **对模型与算法知识的取舍原则**:讲到**资源含义**为止。每个算法概念都要落到"它对应什么资源事实、会改变什么架构决策";数学推导与调参技巧交给外部读物
  - **写**:概念的直觉、它消耗什么资源、它改变什么决策
  - **不写**:公式推导、收敛性证明、效果调优、榜单排名

### 0.3 组织轴

**分层为骨 + 两条主线为脉。**

分部按稳定的抽象层划分(基础认知 → 硬件 → 资源 → 数据 → 训练 → 推理 → 平台 → 治理)。全书用两条主线穿透各层:

- **主线 A**:一次训练任务的全生命周期
- **主线 B**:一次推理请求 / 一次会话的全链路

主线 B 有两个建模单元,必须区分使用:**请求视角**建模单段执行(TTFT/TPOT 的度量与归因单位);**会话视角**建模跨请求的状态与调度(多轮对话、Agent 工具调用会把一次交互切成多段请求,若引擎把每段当独立无状态请求处理,高并发下 KV 会在工具执行期间被驱逐再重算)。两种视角的分工在第 2 章定义;该口径向下约束第 22、24、27、31 章,必须在此处统一,不得等到第 24 章再补。

每章开头必须标注本章位于哪条主线的哪一段。

### 0.4 框架处置原则

**框架不单独立章,只作为各层的实现案例。** 正文只写机制,不写版本与 API;版本信息全部收进附录 C。

| 层 | 训练侧 | 推理侧 | 关心什么 |
|---|---|---|---|
| 运行时 / 引擎层 | Megatron-Core、DeepSpeed、FSDP | vLLM、SGLang、TensorRT-LLM、LMDeploy | 显存、并行、批处理策略 |
| 框架 / 工程层 | TorchTitan、LLaMA-Factory、verl / OpenRLHF | Triton Inference Server、Dynamo | 训练循环、多实例协同 |
| 编排 / 平台层 | Ray Train、Slurm、Kubeflow | KServe、Ray Serve、KubeAI | 弹性、多模型、流量 |
| 网关层 | — | 见第 27 章 | 配额、路由、多供应商 |

> 表中依赖均为纵向;有一处**横向依赖**必须在地图上可见:训练侧工程层的 verl / OpenRLHF 反向依赖推理侧引擎层——RL 的 rollout 要内嵌 vLLM / SGLang(见 §4.2.2「界限正在模糊」)。落图时以跨列虚线或脚注呈现。

这张表放进第 2 章,作为全书的框架地图。

### 0.5 多模态处置原则

**贯穿式插入,不集中成章。** 插入位索引见 §8.2。

### 0.6 硬件与厂商处置原则

| 类型 | 处置 | 位置 |
|---|---|---|
| 具体型号参数 | **正文一律不写**,只写"如何读一张卡的 spec sheet" | 附录 A(标日期) |
| 架构范式 | 正文写,**按范式组织而非按厂商** | §4.2 |
| 国产算力与迁移 | 贯穿式插入 + 独立专章 | §4.2、第 5/7/8/18/22 章 + 第 12 章 |

理由:参数半衰期比书短,范式与迁移方法论则是稳定知识。

### 0.7 阅读路径表(写进前言)

> 本书按**抽象层**组织,读者脑子里却是按**动作**组织的。分层结构不改,但必须补一层动作索引。

| 我想做的事 | 阅读路径 |
|---|---|
| 完全没有算法背景,先补基础 | 3 → 6 |
| 跑通第一次大模型训练 | 3 → 6 → 16 → 17 → 18 |
| 建一个微调平台 | 21 → §19.5 → 25 → 11 → 30 |
| 上线一个推理服务 | 22 → 24 → 23 → 31 → 29 |
| 支撑 Agent / 多轮长上下文业务 | §3.4 → 22 → 24 → 27 → 28 → 31 |
| 把成本压下来 | 11 → 24 → 23 → 31 |
| 迁到国产卡 | §5.3 → 12 → 附录 E |
| 排查性能问题 | §5.4 → §4.4 → 11 |
| 做 RL / 后训练基础设施 | §3.6 → §6.5 → 21 → 20 |
| 支撑多模态业务 | §3.5 → §6.4 → 13 → 24 → 26 |
| 规划一个新集群 | 4 → 7 → 31 |
| 只想先建立全局认知 | 1 → 2 → 3 |
| **给客户做选型建议** | 4 → 12 → 31 |
| **评估一个技术方案是否成立** | 2 → 6 → 对应领域章 → 31 |

**高频动作词在书中的落点**:

| 常用说法 | 对应章节 |
|---|---|
| 算法 / 模型原理 | 第 3 章(定性)+ 第 6 章(定量) |
| 训练 | 第四部分整体(16–21) |
| 微调 | 21(技术)+ §19.5(平台形态)+ 25(LoRA 多租户部署) |
| 部署 | 第五部分(22–26)+ 30(上线流转) |
| 优化 | 23(推理侧)+ 17(训练侧)+ 11(利用率) |
| 评测 | §3.7(概念)+ 29(工具链与流水线) |

### 0.8 本书回答到什么程度(写进前言) 【v8 改写】

> 紧跟路径表之后。本书**讲算法,但只讲到资源含义为止**。

| 问题 | 本书态度 |
|---|---|
| 反向传播是怎么回事、为什么吃显存 | **讲**(§3.1) |
| 反向传播的数学推导 | 不讲,见附录 F 推荐读物 |
| 优化器状态为什么是显存大头 | **讲**(§3.2) |
| 该选 AdamW 还是别的、参数怎么设 | 不讲 |
| batch size 如何影响显存与通信 | **讲**(§3.2) |
| 学习率、warmup 怎么调效果好 | 不讲 —— 明确"这个不归 Infra 管" |
| LoRA 为什么能省资源、部署侧带来什么可能 | **讲**(§3.6) |
| LoRA rank 取多少效果最好 | 不讲 |
| 评测体系怎么构成、工具链怎么接 CI | **讲**(§3.7、第 29 章) |
| 哪个模型在榜单上更强 | 不讲 |

一句话收口:**本书教的是如何让算法同事定下来的方案跑得起来、跑得快、跑得省、跑得稳。**

### 0.9 角色与协作边界索引 【v10 新增】

> AI 平台的很多故障根因是**职责真空**,而不是技术不行。全书已散落多处"谁负责什么"的讨论,这里串成一条线,便于读者遇到"这事该谁干"时一次找齐。
>
> 这是**组织接口**,不是职业发展 —— 每一条都直接来自某个技术决策,不脱离技术单独存在。

| 边界 | 定义处 | 划分的是什么 |
|---|---|---|
| Infra 与算法的超参分工 | §3.2 超参三分法 | 影响资源的你必须有发言权,影响收敛的不归你管 |
| 模型效果验收的归属 | §3.7 + §29.2 | 精度评测谁跑、卡点谁定 |
| 平台与业务方的自助边界 | §19.5 微调即服务 | 模板化到什么程度、失败诊断谁做 |
| 编排层 / 工程层 / 引擎层的职责 | §19.3 | 谁负责进程组、谁负责重启、谁负责数据分发 |
| 利用率问题的归因举证 | 第 11 章 | 低利用率是平台的锅还是任务的锅,靠什么数据判定 |
| 迁移项目的投入归属 | §12.3 | 算子自研、精度对齐、性能调优三段分别由谁承担 |
| 选型决策中的技术与采购分工 | §12.7 + 第 31 章 | 生态成熟度折成人月后,谁来拍这个板 |
| 故障响应的分层 | 第 20 章 | 硬件故障、框架故障、任务代码问题的分诊路径 |

**写作纪律**:上述各处在正文中出现时,必须写清"边界在哪、越界会发生什么",不要写成岗位职责描述。判断标准是——这段话删掉后,读者是否会在真实项目里踩到职责真空的坑;不会,就说明写成了职责描述,应当删掉。

---

## 第一部分 基础与心智模型(7 章)

### 第 1 章 从大数据基础设施到 AI 基础设施

- 大数据时代的基建版图回顾:调度、存储、计算、流批
- 哪些经验可复用:多租户与配额、数据管道、资源隔离、可观测性方法论
- 哪些彻底失效:无状态假设、故障容忍模型、资源粒度、弹性伸缩的前提
- AI Infra 的新约束:显存墙、通信墙、长时任务的全局同步特性
- 全书地图与阅读路径

**交付物**:能说清老经验的复用边界与失效边界,建立"这不是大数据换个引擎"的认知。

### 第 2 章 两条主线解剖与框架地图

- 主线 A:一次训练任务从数据到 checkpoint 的完整链路
- 主线 B:一次推理请求从网关到 token 流的完整链路;请求视角与会话视角(多轮 / Agent)的分工定义
- 每段的主导资源与典型瓶颈标注
- 框架三层地图(见 §0.4)
- 全书术语与符号约定

**交付物**:能画出两条链路图并标出每段主导资源;能把任意一个开源项目放进三层地图的正确格子。

### 第 3 章 人工智能与大模型基础 【v8 新增】

> **定位**:算法概念 → 资源事实的**转译层**。每一节的写法固定为"概念的直觉 → 它消耗什么资源 → 它改变什么决策",不做数学推导。
>
> **与第 6 章的分工**:本章讲**是什么、为什么吃资源**(定性);第 6 章讲**算多少**(定量公式)。同一概念在两处出现不算重复,视角不同。

**3.1 神经网络与训练三阶段**
- 神经网络的基本构成:层、参数、激活值 —— 三个词分别对应后面的哪一块显存
- **前向 / 反向 / 更新三阶段的资源画像**:各阶段的算力、显存、通信特征
- **反向传播**:为什么必须保留前向的激活值 —— 这是激活值显存的来源,也是重计算能省显存的原因(接第 17 章)
- **梯度何时产生、何时同步**:一步训练中通信发生的确切时刻(接第 7 章)
- 数值稳定性的直觉:为什么低精度训练会出问题(接 §4.3)

**3.2 优化器与超参的 Infra 含义**
- 常见优化器的**状态数量差异** → 每参数字节数 → ZeRO 分片的对象到底是什么(接第 17 章)
- **超参三分法**:
  - 影响资源的:batch size、序列长度、梯度累积步数 —— **你必须有发言权**
  - 影响收敛的:学习率、warmup、调度策略 —— 不归你管
  - 两者都影响的:并行相关配置 —— 需要和算法同事共同决策
- 这一节的价值是划清协作边界,而不是教调参

**3.3 从 NLP 到 LLM**
- 语言建模任务的本质:下一个 token 预测 —— 一句话讲完,不展开历史
- **分词与 token**:token 是吞吐、计费、限流、上下文长度的**统一单位**
- 词表大小的资源代价:embedding 与 lm_head 的显存占比、词表并行
- 词向量与嵌入:只讲"它是一张大表"以及这张表有多大

**3.4 Transformer 与注意力**
- Attention 的直觉与形状:Q / K / V、head、序列维度 —— **只讲形状,不讲推导**
- 为什么注意力开销随序列长度快速增长 → 长上下文问题的根源
- **KV Cache 的由来**:自回归解码为什么必须缓存,不缓存会怎样 —— 全书出现频率最高的一个概念,在这里讲清来历
- Encoder / Decoder / Encoder-Decoder 三类结构的现状与资源差异

**3.5 模型谱系**
- **Dense 与 MoE**:稀疏激活的直觉;总参数与激活参数为什么必须分开看(接 §6.2)
- 多模态模型的构成:encoder + projector + decoder 三段式
- 模型规模的量级感:从几 B 到万亿的直观对照

**3.6 训练范式全景**
- **Pretrain / CPT / SFT / RLHF-RL 四阶段**:各自解决什么问题、数据量级差多少、资源特征差在哪
- **RL 的两种形态**:单轮偏好优化(rollout 接近批量推理)vs 多轮 agentic rollout(独立环境服务、轨迹长尾、异步化);定量落 §6.5,基础设施落第 21 章
- **PEFT 与 LoRA**:为什么只训一小部分参数就够用;它在部署侧带来的多租户可能性(接第 25 章)
- 一张"训练范式 → 资源画像"总表(定量细化见 §6.5)

**3.7 评测与 Benchmark 的基本概念**
- **精度评测与性能评测是两套完全不同的东西** —— 这个区分后面反复用到
- 常见能力维度与评测集类型(讲类型,不列榜单)
- Infra 侧为什么必须关心评测:它是模型上线的卡点,也是量化、迁移、优化三件事的验收标准(接第 12、23、29、30 章)

**3.8 推荐读物**
- 想深入推导、调参、了解模型演进史的读者去哪儿

**交付物**:能在与算法同事的对话中准确理解每个术语,并立刻说出它对应的资源含义;能判断一个算法侧决策会不会改变基础设施方案。

### 第 4 章 硬件第一性原理、架构范式与数值精度

**4.1 四个数字与物理约束**

*4.1.1 四个数字*
- 算力、显存容量、显存带宽、互联带宽:四个数字决定一切
- 显存层级:寄存器 / 共享内存 / L2 / HBM,各级带宽与容量差

*4.1.2 HBM:显存墙的物理来源*
- 堆叠结构与 2.5D 封装:为什么显存不能像 DDR 那样加插槽扩容
- 容量与带宽的代际演进曲线,以及它为什么慢于算力增长 —— §4.2"剪刀差"的物理来源
- **HBM 是全行业最紧的供应约束**:解释了高显存卡的溢价,以及国产卡首先卡在哪里(接第 12 章)
- **标称容量 vs 实际可用**:ECC 开销、驱动预留、碎片 —— 标称 80GB 到底能用到多少
- 与第 6 章的接口:显存三分之所以必须精算,是因为这块容量既贵又不可扩

*4.1.3 规模层级:从单卡到超节点*
- 四级层级:单卡 → 单机 → **超节点(统一互联域)** → 多机集群
- **超节点是单机与多机之间新出现的一层**:域内带宽比传统机间高一到两个数量级
- 关键抽象是**互联域**,不是某个厂商的产品形态:域内高带宽、跨域断崖
- 它改写了哪些经典经验:TP 不跨机、All2All 是跨机瓶颈、故障域等于单机、采购单元是"台"
- 具体形态厂商差异大且变动快 —— 正文只写抽象与决策含义,方案对照进附录 A

*4.1.4 功耗与散热:算力密度的物理上限*
- 一句话伏笔:**功耗与散热决定算力能不能持续跑满、一个机柜能塞多少卡;完整讨论见第 31 章**

**4.2 加速器架构范式**

*4.2.1 四类范式*

| 范式 | 代表 | 计算核心 | 编程模型 | 生态成熟度 | 特征 |
|---|---|---|---|---|---|
| SIMT 通用并行 | NVIDIA / AMD GPU | CUDA Core + Tensor Core | 手写 kernel + 库 | 最高 / 中等 | 灵活,生态最厚 |
| 脉动阵列 | Google TPU | MXU | 编译器主导(XLA) | 封闭但完整 | 规则大矩阵极致效率,灵活性差 |
| 多单元协同(达芬奇) | 华为昇腾 | Cube + Vector + Scalar | 图编译 + 算子开发 | 快速追赶中 | 介于两者之间 |
| 推理专用 ASIC | Groq 等 | SRAM 为主 | 编译期静态调度 | 窄而浅 | 超低延迟,容量受限 |

- **核心洞察**:四类范式的真正差异不在峰值算力,而在**编译器承担多少责任**。GPU 把调度权交给运行时,TPU 交给编译器 —— 这直接决定动态形状、变长序列、MoE 路由这些负载在各平台上的难易程度(第 16 章直接引用本条)
- TPU 只讲范式不讲生态
- **生态成熟度是选型的一等变量,不是附加说明** —— 完整的评估维度见 §12.7
- **代际演进规律**:算力增长快于带宽增长 —— 过去十年所有 Infra 优化的根本动因

*4.2.2 训练型与推理型负载的硬件诉求差异* 【v9 新增】

> **本书不采用"训练卡 / 推理卡"这个二分法。** 这个划分过去成立、现在正在失效:同一张卡既跑训练也跑推理已是常态,只是配比不同。真正区分二者的不是芯片标签,而是**§4.1 四个数字的配比**。按厂商标签写"这是训练卡那是推理卡",下一代就失效,且会退化成产品手册。

| 维度 | 训练主导需求 | 推理主导需求 |
|---|---|---|
| 显存容量 | 极高(权重 + 梯度 + 优化器状态 + 激活值) | 中等(权重 + KV Cache) |
| 显存带宽 | 重要 | **决定性**(decode 是 memory-bound) |
| 互联带宽 | **决定性**(集合通信) | 域内够用即可 |
| 精度需求 | BF16 / FP8,需高精度累加 | INT8 / FP8 / INT4,可更激进 |
| 可靠性 | 长时任务,单点故障代价极大 | 无状态,故障可快速替换 |
| 性价比口径 | 每 MFU 成本 | 每 token 成本 |

- **用法**:与其问"这是不是推理卡",不如拿这六行去比对一张卡的 spec sheet —— 这是 §4.2.3 的直接应用
- **推理专用 ASIC 为什么存在**:主动放弃训练所需的灵活性与显存容量,换取延迟与单位吞吐成本;理解了取舍逻辑,就能判断它适不适合你的负载
- **界限正在模糊** 【本节最值得写的一条】:RL 训练把推理(rollout)拉进了训练流程,训练集群里必须跑得动推理引擎;后训练时代"训练卡"和"推理卡"的划分进一步失去意义(接第 21 章混合架构)

*4.2.3 如何读一张卡的 spec sheet*
- 该看的六个数字:各精度峰值算力、显存容量、显存带宽、卡间互联带宽、功耗、单卡价格
- 哪些是营销数字:稀疏算力、理论峰值、不标注精度的 TFLOPS
- 怎么从带宽/算力比反推它更适合哪类负载(配合 §4.2.2 的六行对照)
- 生态维度不在 spec sheet 上,必须单独评估(§12.7)

**4.3 数值格式与精度** 【全书唯一定义处】

> 精度同时决定 Roofline 的两个轴。**本节之后,第 17、22、23 章只讲应用,不再重复定义格式。**

- 位宽的分配逻辑:符号 / 指数 / 尾数;**动态范围 vs 表示精度是两件事**
- 格式谱系:FP32、TF32、FP16、BF16、FP8(E4M3 / E5M2)、FP4、INT8、INT4
- 为什么 BF16 取代了 FP16 —— 赢在动态范围而非精度
- **Tensor Core 的精度-算力倍率表**:每降一档位宽,峰值算力大致翻倍、显存字节数减半
- **记法约定**:W8A8 / W4A16 / W4A8KV4 —— 权重、激活、KV Cache 可各用各的精度
- **累加精度与计算精度的分离**:低精度相乘、高精度累加;长序列求和的灾难性抵消
- INT32 的位置:不作权重格式,但作为累加器类型与索引类型出现(索引溢出见 §5.2)
- 各架构范式的精度支持差异:选型硬约束

**4.4 Roofline 模型**
- 计算密度、拐点、compute-bound 与 memory-bound
- 把 §4.3 的精度代入:同一算子在不同精度下会跨越拐点

**交付物**:能查到一张卡在指定精度下的算力/带宽比,判断算子落在 Roofline 哪一侧;能读懂 W4A16 记法;能只看 spec sheet 判断一张陌生的卡适合什么负载。

### 第 5 章 CUDA、CANN 与加速器软件栈

> 不教写 kernel,只讲到能读懂 profile、能排查版本冲突为止。

**5.1 CUDA 执行模型与软件栈**
- 执行模型:SM、warp、thread block、共享内存与寄存器、occupancy
- 软件栈分层:driver / runtime / toolkit 的版本兼容矩阵;cuBLAS、cuDNN、NCCL、CUTLASS 各自的位置
- 容器里的 CUDA:nvidia-container-toolkit、镜像分层、版本冲突的排查路径
- **Stream 与 CUDA Graph**:为什么小 batch 的 decode 阶段必须靠 Graph 消除 launch 开销

**5.2 运行时的共性问题**
- **显存分配器与碎片**:PyTorch caching allocator 的行为与 OOM 的真实成因
- **索引与偏移量溢出**:大 tensor、长序列、大词表下 INT32 索引静默溢出 —— 生产环境里最难查的一类 bug
- **host 侧瓶颈**:dataloader worker 数、pin memory、GIL 争用、CPU/NUMA 配比 —— GPU 等 CPU 的高频真凶("host 成为瓶颈"的全书落点,数据管道见第 13 章、运行时见第 18 章)
- **PyTorch 作为共同底座**:计算图、算子调度、dispatcher

**5.3 CANN 与昇腾软件栈**
- **完整栈对照**:driver / CANN / 框架适配层 ↔ driver / CUDA / PyTorch
- AscendCL、图引擎(GE)、Ascend C 算子开发、ATC 模型转换各自对应 CUDA 世界的什么
- **图编译模式 vs 单算子下发**:ACL Graph 与 CUDA Graph 的对应关系与差异(接第 23 章图模式优化)
- 昇腾侧的显存模型与常见 OOM 成因差异
- **这不是"CUDA 的一个方言",是两套完整并行的世界** —— 第 12 章迁移评估的前提

**5.4 Profiling 方法论**
- Nsight Systems / torch profiler,如何读时间线定位 bubble
- 昇腾侧 profiling 工具与指标的对应关系
- ROCm 生态简述

**交付物**:能独立读一份 profile 时间线并定位空泡来源;能排查一次版本冲突;能画出 CUDA 与 CANN 两套栈的分层对照图。

**下游引用**:第 17 章显存排查、第 20 章故障检测、第 23 章算子融合与图模式、第 25 章冷启动、第 29 章可观测性。

### 第 6 章 模型结构的定量分析

> 第 3 章讲清了"是什么、为什么吃资源",本章讲"**算多少**"。**这是第五、六部分的入场券。**

**6.1 算子与瓶颈归属**
- Transformer block 的算子构成与形状参数
- **Attention 是 memory-bound、FFN 是 compute-bound**:这决定后面所有优化的方向
- Prefill / Decode 两阶段的定量资源画像差异

**6.2 参数量与资源换算**
- 参数量 → 算力:训练约 6N FLOPs/token、推理约 2N,由此反推训练时长与卡数
- **输入 / 输出 / 缓存命中三态 token 成本换算**:prefill compute-bound 按 prompt 长度累积、decode memory-bound 按输出 token 计,成本严重不对称;请求画像三元组(输入数, 输出数, 前缀命中率)喂给第 27 章计费与第 31 章 QPS→卡数链
- 参数量 → 显存:权重、梯度、优化器状态的每参数字节数(格式定义引用 §4.3,优化器状态数量引用 §3.2)
- 第三类显存:KV Cache 随 batch × seqlen 的线性增长
- **稠密 vs MoE 的总参数 / 激活参数分离**:显存按总参数算、算力按激活参数算 —— 最容易算错的一处
- Scaling 与算力预算:把训练预算折成集群规模与工期(喂给第 31 章)
- **规模谱系表**:每档规模对应的部署形态(单卡 / 单机多卡 / 多机多卡)
- 权重格式与分发:safetensors、分片策略、加载耗时(接第 25 章)

**6.3 结构变体的定量代价**
- MoE:专家负载不均的量化刻画、All2All 通信量
- 长上下文:KV Cache 的线性代价与显存墙触碰点
- **注意力变体的 KV 压缩比**:GQA / MQA / MLA 的 r = a / a_kv,推理容量计算的一等参数(喂给第 22、24、31 章的 KV 估算链)
- 大词表:embedding 与 lm_head 的显存占比计算

**6.4 多模态的定量画像**
- 三段式各自的算力显存占比
- **token 膨胀系数**:一张图 / 一秒音频等于多少文本 token —— 后续所有推理估算的输入

**6.5 训练范式的资源画像量化**
- pretrain / CPT / SFT / RL 各自的数据量、通信模式、资源潮汐(定性版见 §3.6)

**交付物**:给定模型规模、batch、序列长度,能手算显存占用与单步通信量,并判断瓶颈在算还是在搬。公式收进附录 B。

### 第 7 章 互联与集合通信

- 卡间与机间:NVLink / NVSwitch、PCIe、RDMA / RoCE / InfiniBand
- 集合通信原语:AllReduce、AllGather、ReduceScatter、All2All(MoE 的关键)
- 拓扑感知:Ring 与 Tree 算法、NCCL 的拓扑探测与调优
- **互联域与三级拓扑**:超节点把拓扑从两级变成"卡间 / 域内 / 跨域"三级;域边界上的带宽断崖 —— **域边界是全书后半部分反复出现的一条线**
- **HCCL 与昇腾互联**:与 NCCL 的原语对应关系、HCCS 拓扑、组网差异与调优入口
- 通信量估算:各并行策略下的每步通信量公式(区分域内 / 跨域两套带宽参数)
- **低精度通信与梯度压缩**:通信量随精度线性缩减;压缩的收敛代价与适用边界
- 网络故障的表现形式与排查
- **组网设计:把需求变成一张网**:rail-optimized 组网、GPU 与网卡配比(训练网 1:1 无收敛)、PFC/ECN 无损调参与死锁、计算网/存储网/管理网三网分离、网络故障域划分 —— §0.7「规划一个新集群」路径在本章的落点

**交付物**:能看懂三级拓扑图并识别域边界;能估算一次 AllReduce 在指定精度下的通信量与耗时;能对一份组网方案给出五个决策点的评审意见。

---

## 第二部分 算力底座(5 章)

### 第 8 章 K8s 上的加速卡

- 设备插件机制与 GPU Operator
- 资源模型:为什么加速卡不是 CPU,整卡分配的代价
- 拓扑感知调度:NUMA、NVLink 亲和、网卡亲和
- **Ascend Device Plugin 与 NPU 调度**:资源上报、拓扑感知、与 GPU 路径的配置差异
- 节点生命周期:驱动升级、健康检查、故障节点自动隔离

**交付物**:能搭出一个拓扑感知的调度基线,并解释每项配置的性能含义。

### 第 9 章 批调度器

- 为什么默认调度器不够用:Gang Scheduling 与资源死锁
- 队列、配额、优先级、抢占
- Volcano / Kueue / Slurm 的模型差异与选型
- 训练任务与推理服务混部的调度策略
- **混部负载谱系的两类新形态**:离线批量推理(可抢占、无延迟 SLO,填谷首选)与 RL rollout 任务(生命周期介于长时训练与常驻服务之间,弹性配额 + 部分抢占)——集群里增长最快的两类负载

**交付物**:能为一个百卡集群设计队列与配额方案,并说明抢占策略的代价。

### 第 10 章 异构与池化

- MIG、时分复用、vGPU 的适用边界
- 算力池化与远程调用:收益与延迟代价
- 多代硬件共存的调度与性能对齐问题

**交付物**:能判断某个业务场景该不该做切分或池化。

### 第 11 章 多租户与利用率治理

- 配额模型:静态配额、弹性配额、借用与归还
- **MFU 与真实利用率的差距**:为什么 `nvidia-smi` 的利用率是个误导性指标
- **推理侧对等口径**:MFU 对 memory-bound 的 decode 无意义,推理侧用带宽利用率(MBU)、KV 占用率、batch 饱和度、前缀缓存命中率四指标 —— 训练/推理两套口径并列单点定义,喂给第 25、29、31 章
- 利用率归因:数据等待、通信等待、调度空窗、代码低效
- 治理手段:任务画像、闲置回收、超卖策略、用离线批量推理填谷(负载画像见 §9.6)

**交付物**:能建立一套利用率归因体系,把低利用率拆到具体原因上。

### 第 12 章 国产算力平台与异构迁移

> 昇腾双轨处理的"轨二"。前面各章按层插入技术对照,本章集中解决**决策与迁移**。中文技术书里几乎空白的一块。

**12.1 国产算力版图**
- 昇腾、寒武纪、海光、燧原等的定位差异与生态成熟度(只讲定位,不讲参数)
- 训练侧与推理侧的成熟度不对称:为什么推理先落地、训练更难

**12.2 迁移的三道坎**
- **第一道 算子覆盖率**:如何快速摸清模型用到的算子有多少已支持;缺失算子的四条出路(等待厂商 / 等价替换 / 自研 kernel / **兼容层与翻译层**——第四条须写清失效边界:永远滞后一个版本、性能天花板、适配节奏绑死在别人身上)
- **第二道 精度对齐**:逐层比对方法、累积误差定位、什么样的差异可以接受(评测口径引用 §3.7)
- **第三道 性能调优**:算子融合策略差异、内存搬运模式差异、通信参数重调
- **90% 的项目卡在第三道** —— 能跑通不等于能用

**12.3 迁移评估方法论**
- 如何在两周内判断:能不能迁、性能损失多少、需要投入多少人力
- 评估清单与打分表(完整版见附录 E)
- 什么样的模型不该迁

**12.4 混合集群管理**
- NVIDIA 与国产卡共存时的调度、镜像、监控统一
- 一套代码两套后端:抽象层放在哪一层最省事;CI 如何覆盖双后端

**12.5 性能对标的正确姿势**
- 为什么单卡跑分没有意义
- 要看端到端 MFU、集群规模效率、**每元算力的有效吞吐**
- 对标实验的设计:控制变量、报告口径、常见的不诚实比法

**12.6 供应链与生命周期视角**
- 采购周期、驱动支持年限、生态迭代速度对技术选型的实际约束

**12.7 生态作为选型变量** 【v9 新增】

> 生态在 §4.2、§5.3 里都被提到,但那里是背景;在这一章它是**核心论据**。本节把"生态好不好"这句模糊的话拆成可评估的维度。

- **可评估的五个维度**:
  - 算子与模型覆盖率,以及**更新滞后期** —— 新模型出来多久能跑起来
  - 主流框架的适配深度:官方支持、厂商维护分支,还是社区补丁
  - **文档、报错信息、社区问答的可得性** —— 这一项直接折算成工程师工时
  - 人才供给:市场上招得到会调的人吗
  - 迭代速度与向后兼容记录:升级会不会推翻你的适配工作
- **硬观点**:换硬件的真实成本里,**硬件差价往往不是大头,生态成熟度折算的工时才是**。一个覆盖率 90% 的平台和一个 99% 的平台,差的那 9% 可能吃掉整个采购节省
- 如何量化:把上述维度折成人月,并进第 31 章的 TCO 模型
- 生态的正反馈:为什么落后者追赶困难,以及什么条件下能被打破
- 对采购决策的实际建议:小规模试点先验证生态而非性能

**交付物**:能独立完成一次迁移可行性评估并出具人力与工期估算;能设计一套双后端并存的工程方案;**能把"生态成熟度"折算成可进 TCO 模型的数字**。

---

## 第三部分 数据底座(3 章)

### 第 13 章 训练数据管道

> 大数据经验密度最高的一章。

- 采集、清洗、去重(精确与近似)、质量过滤
- 数据配比、课程学习、采样策略的工程实现
- 合成数据的生产流水线与污染防控
- 数据版本与可复现性
- **续训一致性**:dataloader 状态(数据流位置、shuffle RNG)必须与模型状态同点保存,否则重复消费或漏样本;确定性推导 / 显式状态 / 样本账本三档实现(写路径接第 14 章、恢复接第 20 章)
- **边界声明**:RL 后训练数据由 rollout 现场产生,不走本章管道 → 第 21 章;种子提示集仍从本章出
- **非文本数据管道**:体量比纯文本高两三个数量级,瓶颈从 IO 转到 CPU 解码;NVDEC 硬解、DALI、WebDataset 分片、预处理与训练的流水线重叠
- **数据飞轮** 【v8 新增】:线上请求回流 → 筛选与脱敏 → 标注 → 进训练集 → 模型更新 → 再回流
  - 回流采样率与存储成本
  - 隐私边界与合规:哪些能留、留多久
  - **污染防控**:线上数据可能含模型自己的输出,自噬问题的工程防线
  - 与第 29 章质量监控、第 30 章生命周期的接口

**交付物**:能定位数据侧是否是训练吞吐的瓶颈;能设计一条合规可用的数据回流闭环。

### 第 14 章 存储与 Checkpoint I/O

- 对象存储、并行文件系统、本地 NVMe:三层存储的分工
- 缓存加速层:Alluxio / JuiceFS 类方案的适用场景
- **Checkpoint 的突发写模式**:为什么它和常规负载完全不同
- **读路径三类画像**:续训并行加载(含拓扑变化的重分片读放大)、评测高频小读、导出转换(训练格式 → 推理格式,接第 30 章)
- **最优 checkpoint 间隔**:与第 20 章失败率模型联立,τ* ≈ √(2CM) 可代入公式进附录 B(F22)
- 异步 checkpoint、分片保存、增量保存
- 小文件问题在 AI 场景的重演形式

**交付物**:能为给定集群规模设计存储分层与 checkpoint 策略,并估算写入窗口。

### 第 15 章 向量与检索基础设施

- 索引结构:HNSW、IVF、量化索引的取舍
- 增量更新、删除、一致性
- 混合检索与召回链路的工程实现
- **embedding / rerank 小模型服务的资源画像**:高 QPS、极小 batch、无 KV Cache;CPU 与 GPU 的可算边界、与主模型混部的隔离要求 —— 属检索链路,不归第 25 章的 LLM 弹性逻辑
- 规模与成本:内存占用估算、分片与副本

**交付物**:能按数据规模与召回要求选出索引方案并估算资源。

---

## 第四部分 训练系统(6 章)

### 第 16 章 并行策略

- DP / TP / PP / EP / SP 的原理(引用第 6 章的形状分析)
- 各策略的通信量与显存收益公式
- 3D / 4D 并行的组合选型与搜索空间
- **超节点如何改写并行选型**:TP 域可扩到远超 8 卡,"TP 不跨机"直接作废;MoE 的 All2All 从跨机瓶颈变为域内操作,EP 可行规模大幅上移;并行切分与域边界的对齐规则
- 流水线气泡与调度算法
- **变长与异构负载下的并行**:padding 浪费与 sequence packing;encoder 与 decoder 采用不同并行策略(引用 §4.2 的"编译器责任"结论)
- 并行原语在各框架中的 API 映射

**交付物**:能为给定模型和集群规模选出并行组合,并估算 MFU 上限。

### 第 17 章 显存与训练侧精度机制

> 格式定义已在 §4.3 完成,本章只讲**训练侧的精度机制**。

- ZeRO 三个阶段的原理与代价(分片对象引用 §3.2 优化器状态)
- 激活重计算的粒度选择(原理引用 §3.1 反向传播)
- CPU / NVMe Offload 的收益边界
- **混合精度训练机制**:master weights、loss scaling、per-tensor / per-block scaling
- **FP8 训练的工程门槛**:哪些层能降、哪些必须留高精度、scaling 因子更新策略
- **数值发散的排查路径**:loss spike、NaN 溯源、精度回退的诊断顺序
- 显存 OOM 的系统性排查路径(引用 §5.2 分配器)

**交付物**:能在给定卡数下把某模型"塞进去",说明每项技术的吞吐代价;能诊断一次低精度训练的数值发散。

### 第 18 章 并行运行时剖析

- Megatron-Core、DeepSpeed、FSDP2 的机制差异
- 描述维度固定为:调度策略、显存管理、并行支持、扩展点、失效边界
- 组合使用的边界与常见冲突
- **昇腾训练路径**:MindSpeed / ModelLink 的定位;PyTorch + torch_npu 与 MindSpore 两条路线的取舍
- 何时该自己写训练循环,何时不该

**交付物**:能按机制差异而非流行度选出运行时,并说明其失效边界。

### 第 19 章 训练工程层与编排

**19.1 工程层**:TorchTitan、LLaMA-Factory / Axolotl 的定位与适用规模

**19.2 编排层**:Ray Train、Slurm、Kubeflow 的分工

**19.3 三层接口契约**:谁负责进程组、谁负责重启、谁负责数据分发

**19.4 实验管理与超参搜索的基础设施支撑**

**19.5 微调即服务的平台形态**

> 第 21 章讲微调作为**技术**,本节讲微调作为**服务**。

- **调度诉求相反**:小任务多、突发强、生命周期短,与大规模预训练的长时独占刚好对立
- **数据隔离与权限**:业务方数据互不可见,存储、镜像、日志三条路径都要隔离
- **模板化**:把并行策略、超参默认值、显存配置封装成"选个基座 + 传个数据集就能跑"
- **产出物管理**:几百个 LoRA 权重的注册、评测、上线流转(接第 25、30 章)
- **成本归属**:按 token 还是按卡时计费,两种口径对业务方行为的不同引导
- 失败任务的自助诊断

**交付物**:能画出从编排层到引擎层的职责边界图;能设计一套支撑多业务方自助微调的平台方案。

### 第 20 章 稳定性与容错

- 长时任务的失败率模型:为什么千卡规模下故障是常态
- 故障检测:hang 检测、集合通信超时、静默错误
- 慢节点识别与踢除
- **故障域的放大**:超节点下一次域内故障影响面从 8 卡变为上百卡,失败率模型、重调度粒度、备机策略都要重算
- 弹性训练与断点续训
- **低精度下的可复现性**:归约顺序的非确定性、变卡数续训的结果漂移
- 故障演练与 SLO

**交付物**:能设计一套故障检测与自动恢复流程,并估算有效训练时间占比。

### 第 21 章 后训练基础设施

- SFT / LoRA / DPO / RLHF 各自的资源画像(概念引用 §3.6,定量引用 §6.5)
- **RL 训练框架专节**:verl、OpenRLHF、TRL、slime —— 重点是它们如何解决 rollout 与 train 的资源潮汐
- 混合架构:训练实例与推理实例共存、权重同步、显存倒换
- 奖励模型与环境服务的基础设施
- 当下资料最稀缺、也最值得写深的一章

**交付物**:能设计一套 RL 训练的资源编排方案,并说明潮汐期的资源利用策略。

---

## 第五部分 推理系统(5 章)

### 第 22 章 推理引擎原理

**22.1 KV Cache 与显存**
- 结构、增长规律、显存占比(由来见 §3.4)
- **KV Cache 精度的系统影响**:降精度如何同时改变显存占用、可容纳并发数与前缀缓存命中率(算法侧见第 23 章)
- PagedAttention 与显存碎片治理

**22.2 批处理与调度**
- Continuous Batching 与调度器设计
- **Chunked Prefill** 【v8 新增】:长 prompt 把 decode 请求饿死是所有推理服务都会遇到的问题;分块的原理、块大小选择、以及它对 TTFT 与 TPOT 的权衡影响
- 优先级、抢占与请求驱逐

**22.3 采样与后处理阶段** 【v8 新增】
- 贪婪采样(Greedy)、Top-k / Top-p、温度、repetition penalty 各自在做什么
- **Infra 侧的关键事实**:大词表下 sampling 是 memory-bound 的,logits 张量本身很大,后处理在 decode 阶段的耗时占比常被严重低估
- 结构化输出与约束解码:引入的 CPU 侧串行与吞吐代价
- Stop 条件、流式输出与首 token 返回的工程细节

**22.4 引擎剖析**
- vLLM、SGLang、TensorRT-LLM、LMDeploy 的调度器与内存管理差异
- **昇腾推理路径**:MindIE 与 vLLM-Ascend 的现状、能力边界与选型
- 边缘侧:llama.cpp / Ollama 的不同取舍

**22.5 加速特性三分法(第五部分地图)** 【v8 新增】

| 类别 | 手段 | 优化的是什么 | 展开位置 |
|---|---|---|---|
| **调度类** | Continuous Batching、Chunked Prefill、优先级抢占 | 吞吐与排队延迟 | §22.2 |
| **显存类** | PagedAttention、Prefix Cache、KV 量化、KV 池化 | 并发上限 | §22.1、第 24 章 |
| **计算类** | 算子融合、图模式、量化、投机解码 | 单步耗时 | 第 23 章 |

> 读者拿到任何一个新特性都应能立刻归类,并知道它和哪些特性冲突 —— **调度类与显存类经常互相挤兑**。这条分类同样适用于第 17 章的训练侧。

**交付物**:能解释同一模型在不同引擎上吞吐差异的机制来源;能把任意一个新出现的加速特性归入三分法并判断其冲突面。

### 第 23 章 推理优化:计算类加速

> 格式与记法已在 §4.3 定义,本章只讲**推理侧的量化算法与计算加速**。

**23.1 量化**
- 量化算法:PTQ 与 QAT、逐通道 / 逐组量化、离群值处理
- W / A / KV 三者分别降精度的收益差异与组合选型(记法见 §4.3)
- **量化工具链**:GPTQ / AWQ / llm-compressor / TensorRT Model Optimizer
- **精度评估**:如何验证量化后没有掉点,评测集的选择陷阱(工具链见第 29 章)

**23.2 算子与融合**
- 编译与算子层:Triton 语言、TorchInductor、FlashAttention / FlashInfer、CUTLASS
- **昇腾侧融合特性** 【v8 新增】:VF 融合、CF 融合等融合模式的适用条件与收益;与 GPU 侧融合思路的异同
- 自定义算子的开发与维护成本

**23.3 图模式**
- CUDA Graph 与 **ACL Graph**:消除 launch 开销的原理与限制(基础见 §5.1、§5.3)
- 动态形状与图模式的天然冲突,以及工程上的折中

**23.4 投机推理**
- 草稿模型、Medusa 类方法、接受率与收益模型
- 与调度类特性的相互影响

**23.5 叠加与冲突**
- 各项优化的叠加效应与相互冲突矩阵
- 优化顺序的建议路径

**交付物**:能按 SLO 与精度约束排出一条优化路径,并预估各项收益与冲突。

### 第 24 章 服务化架构与显存类加速

- **PD 分离**:动机、KV 传输代价、适用边界
- **域内 PD 分离**:超节点下 KV 传输代价急剧下降,分离的收益模型随之改变;跨域分离为什么依然不划算
- **Prefix Cache 与 KV Cache 多级复用**:前缀缓存、跨请求复用、显存/内存/远端三级
- **KV Cache 池化**:跨实例共享的架构与一致性代价
- 前缀感知路由与负载均衡
- **encoder 与 LLM 的分离部署**:多模态 prefill 极重,PD 分离收益比纯文本更大;encoder 输出缓存复用
- 编排层:Triton Inference Server、Dynamo、KServe、Ray Serve 与引擎层的接口契约

**交付物**:能根据 TTFT / TPOT 的 SLO 反推出部署形态与卡数。

### 第 25 章 弹性与多模型

- 冷启动:权重加载、图编译、预热的时间构成与优化
- 扩缩容:指标选择、滞后问题、预热池
- 多模型共存与 **LoRA 多租户**(原理见 §3.6,平台侧见 §19.5)
- 模型热更新与灰度切换

**交付物**:能设计一套满足冷启动 SLO 的弹性方案。

### 第 26 章 特殊负载

- 长上下文:显存墙、注意力复杂度、分块与卸载策略
- **Diffusion**:迭代去噪、compute-bound、显存随分辨率 × 帧数增长,批处理策略与自回归完全相反
- **实时语音**:端到端延迟预算的逐段拆解
- 三类负载与文本 LLM 在架构决策上的分歧点汇总

**交付物**:能识别出何时不能套用文本 LLM 的部署经验。

---

## 第六部分 上层平台(3 章)

> 本部分只讲基础设施接口,Agent 的架构设计交给《企业级 Agent》。

### 第 27 章 模型网关

- 统一 API 层与协议适配
- 限流、配额、计费的 token 级实现(token 口径见 §3.3)
- 多供应商路由、故障转移、降级策略
- 缓存与去重

**交付物**:能设计一套支撑多模型多供应商的网关架构。

### 第 28 章 Agent 运行时基础设施

- 沙箱与代码执行:隔离级别、启动延迟、资源限制
- 工具调用的基础设施:超时、重试、并发
- 会话状态与上下文存储
- RAG 的基础设施视角(引用第 15 章)

**交付物**:能为 Agent 平台设计出运行时的资源与隔离方案。

### 第 29 章 可观测性与评测流水线

**29.1 可观测性**
- **LLM 专属指标体系**:TTFT、TPOT、吞吐、并发、MFU、缓存命中率
- 指标 / 日志 / Trace 在 AI 场景的特殊性
- 训练侧可观测性:引用 §5.4 profiling 方法论
- 告警设计与根因定位路径

**29.2 评测工具链** 【v8 新增】
- **精度评测与性能评测的分工**(概念见 §3.7):两套体系、两套指标、两套基础设施
- 精度评测工具:OpenCompass、EvalScope 等的定位与差异
- **AISBench**:兼具精度与性能 benchmark 能力的定位
- **Infra 侧真正关心的三件事**:评测任务本身的调度成本(它也是个吃卡的批任务)、结果的可复现性、以及如何接进 CI 做回归
- 评测流水线:回归评测、A/B、线上质量监控
- 谁来跑评测:平台提供能力还是业务方自持

**交付物**:能建成一套覆盖模型层到应用层的指标与告警体系;能把评测接进 CI 成为上线卡点。

---

## 第七部分 工程化与治理(2 章)

### 第 30 章 模型生命周期管理

- 模型注册、版本、血缘
- 灰度、回滚、影子流量
- 制品管理与权重分发(接第 25 章冷启动)
- 评测与上线的准入卡点(工具链引用 §29.2)
- 与数据飞轮的闭环接口(接 §13)

**交付物**:能设计一套从训练产出到线上服务的完整流转链路。

### 第 31 章 容量、成本与 SLO

**31.1 容量与压测**
- 容量模型:从业务指标反推卡数(引用 §6.2 换算公式)
- **采购粒度的变化**:超节点使最小采购单元从"台"变为"域/柜",容量规划的台阶变粗
- 压测方法论:负载建模、长尾、饱和点
- SLO 定义与容量水位

**31.2 机房、功率与散热**

> §4.1.4 埋的伏笔在此展开。

- **功耗密度是根因**:单卡功耗翻倍后,单机柜功率从十几千瓦跃升到上百千瓦,风冷在某个密度点直接失效
- 三种方案的适用密度区间与改造成本:风冷 / 冷板式液冷 / 浸没式液冷
- **超节点必然伴随液冷**:高密度部署是超节点的物理前提
- **降频与热节流**:压测跑得好、跑一小时掉性能 —— 很多"性能不稳定"问题的真凶
- PUE 与电费如何进入 TCO 模型
- 老机房改造的现实约束:承重、配电、水路,及其对硬件选型的反向锁定

**31.3 成本**
- TCO 建模:硬件、电力、机房改造、运维人力
- 自建 vs 云 vs 混合的决策框架;**国产卡在 TCO 模型里的正确算法**(引用 §12.5 对标口径)
- 成本归因与优化杠杆排序

**交付物**:能出具一份包含容量、成本、SLO 三者约束的平台方案,并正确计入功率与散热约束。

---

## 附录

### 附录 A 加速卡与集群形态速查表
- 主流加速卡的显存、带宽、互联参数对照
- **各精度下的峰值算力分列**,与附录 B 精度表对齐
- **负载适配倾向标注** 【v9 新增】:按 §4.2.2 六维配比给出"偏训练 / 偏推理 / 通用"的倾向标记 —— **是倾向标注,不是非此即彼的分类**
- **HBM 代际对照**:容量、带宽、标称与实际可用量差额
- **超节点形态对照**:各方案的单域卡数、域内互联带宽、域间方案
- **功率密度参考值**:单卡功耗、机柜功率、对应可行的散热方案
- **国产卡单列一节**
- 页首标注数据日期

### 附录 B 估算公式速查与符号表
- 第 6、7、16、17 章所有公式汇总:显存占用、通信量、吞吐、MFU、训练时长、KV Cache 大小
- **精度对照表**:格式 × 每元素字节数 × 动态范围 × 有效位数 × 硬件支持代际 × 典型用途
- 全书符号统一定义

### 附录 C 框架选型快照
- 三层框架地图的完整项目清单(含昇腾侧对应项目)
- **选型矩阵**:按场景 × 规模给出推荐
- 页首标注"截至 XXXX 年 X 月",附在线勘误与更新地址

### 附录 D 术语表
含 CUDA 与 CANN 术语的双向对照表,以及算法术语的资源含义速查。

### 附录 E 异构迁移评估清单
第 12 章配套工具:算子覆盖率盘点表、精度对齐检查项、性能对标实验设计模板、人力与工期估算打分表。

### 附录 F 算法深入读物推荐 【v8 新增】
第 3 章的延伸出口:想深入数学推导、模型演进史、调参实践的读者去哪儿。**明确标注本书不覆盖这些内容是有意为之。**

---

## 八、写作纪律

### 8.1 章节能力，不是固定目录

每章都要让读者知道当前问题位于哪条资源链路、用了哪些前文口径、如何验证结论以及何时失效，但这些内容不使用统一标题，也不规定出现顺序。章节标题应表达具体判断，例如“调度结果先划定物理边界”，而不是“链路定位”或“依赖声明”。

结构检查只验证以下能力是否存在：

1. 用自然导语或简短上下文卡片交代本章问题；
2. 在前文结论首次被使用时就地链接，不集中罗列前置章节；
3. 从一个可核验的生产困境或容量问题进入机制；
4. 给出公式、时间线、实验或其他可复现证据；
5. 比较候选方案，并说明各自的前提和失效边界；
6. 说明旧平台经验在何处可复用、何处会失效；
7. 以工作表、检查表、决策流程或回退条件收束。

第 3 章作为基础知识地图，继续使用“概念直觉 → 资源事实 → 决策含义”的三段式。

### 8.2 贯穿式主题插入位索引

**多模态**

| 章节 | 插入内容 |
|---|---|
| §3.5 | 多模态模型构成(定性) |
| §6.4 | 多模态定量画像、token 膨胀系数 |
| 第 13 章 | 非文本数据管道 |
| 第 16 章 | 变长异构负载并行 |
| 第 24 章 | encoder 分离部署 |
| 第 26 章 | 收口:Diffusion 与实时语音 |

**昇腾 / 国产算力**

| 章节 | 插入内容 |
|---|---|
| §4.2.1 | 达芬奇架构范式与生态成熟度 |
| §5.3 | CANN 栈完整对照 |
| 第 7 章 | HCCL 与 HCCS |
| 第 8 章 | Ascend Device Plugin |
| 第 18 章 | MindSpeed / MindSpore |
| §22.4 | MindIE / vLLM-Ascend |
| §23.2、§23.3 | VF / CF 融合、ACL Graph |
| **第 12 章** | **专章收口** |

**超节点 / 互联域**

| 章节 | 插入内容 |
|---|---|
| §4.1.3 | 规模层级 |
| 第 7 章 | 互联域与三级拓扑 |
| 第 16 章 | 并行选型改写 |
| 第 20 章 | 故障域放大 |
| 第 24 章 | 域内 PD 分离 |
| 第 31 章 | 采购粒度与散热 |

### 8.3 单点定义原则

横切全书的基础概念只在一处定义,其余章节一律引用:

| 概念 | 定义处 | 引用方 |
|---|---|---|
| 算法概念的资源含义 | 第 3 章 | 全书 |
| 反向传播与激活值显存 | §3.1 | 第 17 章 |
| 优化器状态与超参三分 | §3.2 | 第 17、19 章 |
| token 口径 | §3.3 | 第 27、31 章 |
| KV Cache 由来 | §3.4 | 第 22、24 章 |
| 精度评测 vs 性能评测 | §3.7 | 第 12、23、29、30 章 |
| HBM 与显存墙 | §4.1.2 | 第 6、12、22 章 |
| 规模层级与互联域 | §4.1.3 + 第 7 章 | 第 16、20、24、31 章 |
| 架构范式与编译器责任 | §4.2.1 | 第 12、16 章 |
| 训练/推理负载的硬件配比 | §4.2.2 | 第 21、22、31 章 |
| 生态评估维度 | §12.7 | §4.2.1、第 31 章 |
| 数值格式与精度记法 | §4.3 | 第 6、7、17、20、22、23 章 |
| Roofline 与瓶颈判定 | §4.4 | 第 6、16、22、23 章 |
| 显存三分与参数换算 | §6.2 | 第 16、17、24、31 章 |
| profiling 方法论 | §5.4 | 第 17、20、29 章 |
| 利用率口径:训练 MFU / 推理 MBU、KV 占用、batch 饱和、前缀命中 | §11.3.2 | 第 25、29、31 章 |
| 框架三层地图 | §0.4 | 第 18、19、22、24 章 |
| 加速特性三分法 | §22.5 | 第 23、24 章 |
| 性能对标口径 | §12.5 | 第 11、31 章 |
| 功率与散热约束 | §31.2 | §4.1.4 埋伏笔 |
| 本书边界(写/不写) | §0.2 + §0.8 | 全书 |
| 角色与协作边界 | §0.9 | 第 11、12、19、20、29、31 章 |

校验脚本可加一条检查:正文出现这些概念的"定义式表述"时,若不在定义章内,应改为引用。

> 这张表已有 21 条,建议单独拆成一个维护文件,正文只留引用。

### 8.4 抗过时规则

- **正文只写机制,不写版本号与 API 签名**
- 框架描述维度固定为:调度策略、显存管理、并行支持、扩展点、失效边界
- **具体卡型号与参数一律不入正文**,只写"如何读 spec sheet"的方法论
- 所有会过时的清单与参数集中在附录 A / C,标注日期
- 定稿前需重新核对:框架现状(尤其 RL 训练与 PD 分离编排)、各平台生态成熟度与算子覆盖率、精度支持矩阵、超节点形态与 HBM 代际、评测工具链现状

### 8.5 章节交付物制度

每章必须给出可验证的"交付物"(读者读完能算出什么 / 能做出什么判断)。写作时先定交付物,再倒推内容 —— 凡是不服务于交付物的内容一律删掉。

### 8.6 第 3 章的特别纪律 【v8 新增】

算法内容进正文后,最大的风险是**写着写着变成入门教程**。两条硬约束:

1. **每一节必须以"它消耗什么资源 / 它改变什么决策"收尾**,写不出这句话的内容整节删掉
2. **不出现公式推导**,只出现形状、量级、比例关系
---

## 九、体量评估

| 部分 | 章数 | v8 变化 |
|---|---|---|
| 一 基础与心智模型 | 7 | **+1**(第 3 章 AI 与大模型基础) |
| 二 算力底座 | 5 | 顺延为 8–12 |
| 三 数据底座 | 3 | 顺延为 13–15 |
| 四 训练系统 | 6 | 顺延为 16–21 |
| 五 推理系统 | 5 | 顺延为 22–26 |
| 六 上层平台 | 3 | 顺延为 27–29 |
| 七 工程化与治理 | 2 | 顺延为 30–31 |
| **合计** | **31** | v7 为 30 |

**若需压缩**,顺位建议:第 10 章并入第 9 章 → 第 15 章并入第 28 章 → 第 26 章的实时语音一节下沉附录。第一部分、第 12 章、第四五部分核心章不建议压缩。

**已登记的结构调整(待执行)**:第 4 章现为两章之量(4.1 物理约束 + 4.2 架构范式为一章;4.3 精度谱系 + 4.4 Roofline 为一章),且 §4.3 是全书 6 章引用的唯一定义处,埋在硬件章第三节不利检索。计划:启用上行压缩顺位第一条(第 10 章并入第 9 章)腾出章号,「数值精度与 Roofline」独立成章。该变更涉及全书对 §4.3 / §4.4 的入链迁移、章号与文件路径变更,必须按 governance/migration-policy.md 的旧路径登记流程单独成 PR 执行,不与内容修订混在一起。
