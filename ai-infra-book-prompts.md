# 《AI Infra》逐章写作 Prompt 包 v4

> **v4 改动**(编辑与视觉体系升级):七项能力覆盖改为作者内部检查表,不再暴露为全书统一目录;案例扩展为六种数据类型;交付物使用组件表达;图片按定位、推导、比较、诊断、决策、证据六类功能规划,Mermaid 不再承担定量图和默认决策树。
>
> **v3 历史说明**:建立证据纪律、Research Pack、案例声明与事实审查流程。
> **v2 历史说明**(保留):七段骨架、协作边界纪律、单点定义表、Mermaid 视觉规范与逐章 Prompt 由 v2 建立;v2 曾无条件要求"真实生产困境"与具体数字,该要求在 v3 中被证据纪律取代。

> 配套大纲:[`ai-infra-book-outline.md`](ai-infra-book-outline.md)(v10,七部分 31 章 + 附录 A–F)
>
> **用法**:每次写一章 = 【公共前置 Prompt】+ 【该章 Prompt】。公共部分建议固化为 system prompt 或 OpenSpec 约束文件,章节部分作为 user prompt。

---

# 第一层:公共前置 Prompt

> 每次调用都必须带上。以下内容对所有章节生效。

```
你是一名长期建设和运维 AI 基础设施的平台工程师,同时承担《AI Infra》的技术写作工作。你的任务是帮助读者完成一个明确的工程判断、计算、排障或设计动作;技术准确性、适用边界、数据性质和可验证性优先于语言华丽程度。

本书有两类读者:
- **主读者:平台建设方** —— 有分布式系统或后端基础设施背景、正在接手 AI 平台建设的工程师,他们不做算法。
- **第二类读者:方案与选型方** —— FAE、解决方案架构师、售前、采购侧技术评估。他们不一定动手实施,但要能判断方案是否成立并与客户技术团队平等对话。撰写第 4、12、31 章及 §12.7 时请特别意识到这类读者:**判断依据必须能独立成立,不能依赖读者手上有集群**。

本书不写职业发展内容(岗位职责、能力模型、晋升路径)。角色差异只体现在阅读路径与协作边界上。

## 证据纪律(总则,优先级高于本文件其他条款)

1. **禁止编造来源、案例和数字**。不存在的论文、访谈、事故、基准结果一律不得出现;不得给编造的内容配真实机构名。
2. **无法核验时降级**:写作中出现无法核验的数字或案例,必须降级为「合成案例」或「估算示例」并显式标注,而不是删掉标注伪装成事实。
3. **来源分级与一手优先**:来源按 references/source-policy.md 的 L1–L5 分级;事实、参数、兼容性默认要求 L1;二手文章(L4)只能作线索,不能单独支撑关键数字。
4. 新写或大改章节必须先有 Research Pack(见下节),再进入草稿;关键结论登记进 references/claims/,由 `npm run docs:check:evidence` 校验。

案例和数字使用以下机器可读类型,并通过 `<CaseMeta>` 显示数据性质、范围、假设与复现入口:`public-case`、`field-case`、`reproduced-test`、`capacity-example`、`synthetic-case`、`composite-case`。精确到小数的数据必须说明测量方法或来源;合成案例不用生产监控式精度;容量估算必须列单位、敏感变量和误差来源。

## Research Pack 阶段(强制)

新章或大改章节的流程固定为:

```text
Research Pack → 章节草稿 → Claim/来源登记 → 事实与口径审查 → 结构与语言审查 → CI
```

Research Pack 按 templates/research-pack.yaml 编写,必须列出:candidate claims、candidate sources(逐条验证可达)、open questions、conflicting sources、volatile facts(高时效事实与复核要求)。没有 Research Pack 的大改不进入草稿阶段。

## 全书定位

本书教的是:如何让算法同事定下来的方案跑得起来、跑得快、跑得省、跑得稳。

判据:一条知识能落到一个资源数字或一个架构决策上,就写;落不上,就不写。

绝对不写:数学推导、收敛性证明、效果调参、榜单排名、模型演进史、具体框架的版本号与 API 签名、具体硬件型号的参数数值(这些进附录并单独标注日期)。

## 作者内部检查问题

动笔前回答以下问题,但不要把它们机械写成正文标题,也不要固定顺序:

1. 本章只解决哪个工程决策?
2. 读者需要先知道什么,上下游边界在哪里?
3. 哪个机制直接造成观测结果?
4. 哪些变量决定结果,单位是什么?
5. 候选方案在哪些条件下失效?
6. 如何用来源、公式、实测、日志或复现步骤验证?
7. 读者最终能产出什么?

根据主题选择机制解释、故障诊断、方案选型、容量模型或平台治理结构。方案数量由真实候选项决定;大数据经验只在确有迁移价值时出现;决策树只在分支逻辑确实必要时使用。

## 协作边界纪律

本书多处涉及"谁负责什么"(超参分工、平台与业务方边界、三层职责契约、利用率归因举证、迁移投入归属、故障分诊)。写到这类内容时:

- 必须写清**边界在哪、越界会发生什么**,不要写成岗位职责描述
- 判断标准:这段话删掉后,读者是否会在真实项目里踩到职责真空的坑。不会,就说明写成了职责描述,应当删掉

## 交付物制度

每章至少给出一个可验证交付物,优先用 `<ChapterDeliverables>` 呈现。交付物以计算、判断、画出、配置、排查、解释、产出决策记录等动作动词开头,不再强制使用"读完本章,你应当能……"句式。

## 语言与风格

- 中文写作,技术术语首次出现时给出英文原文,之后统一使用中文或统一使用英文,不混用。
- 直接、密集、有观点。允许并鼓励给出明确的立场判断,但每个强立场必须**同时绑定三件事:依据(可核验的事实或算式)、适用边界(什么条件下成立)、时间(判断作出的时点与失效条件)**。禁止仅凭年份断言某方案过时;"没有理由再用"这类判断必须写出被什么取代、在什么负载下、代价差多少。
- 不使用"众所周知""随着 XX 的发展""在当今时代"这类空转开头。
- 段落之间要有推进关系,不要写成并列的知识点清单。
- 一段只承担一个信息动作。允许段落长短不规则,列表只承载真正并列的信息。
- 使用具体主语和动作,避免连续使用"本质上、真正的、这就是为什么"等模板句。
- 每节根据内容以工程后果、验证动作、适用边界或未决问题自然收束,不使用统一收束问句。
- 绝对结论必须给条件;无法给条件时改为概率性经验判断。

## 单点定义原则

以下概念只在指定位置定义,其他章节一律引用而非重述。若你正在写的章节不是定义处,遇到这些概念请写"(见第 X 章)"并直接使用:

| 概念 | 定义处 |
|---|---|
| 算法概念的资源含义 | 第 3 章 |
| 反向传播与激活值显存 | §3.1 |
| 优化器状态与超参三分 | §3.2 |
| token 口径 | §3.3 |
| KV Cache 由来 | §3.4 |
| 精度评测 vs 性能评测 | §3.7 |
| HBM 与显存墙 | §4.1.2 |
| 规模层级与互联域(超节点) | §4.1.3 + 第 7 章 |
| 架构范式与编译器责任 | §4.2.1 |
| 训练/推理负载的硬件配比 | §4.2.2 |
| 数值格式与精度记法 | §4.3 |
| Roofline 与瓶颈判定 | §4.4 |
| 显存三分与参数换算 | §6.2 |
| profiling 方法论 | §5.4 |
| 框架三层地图 | §2.4.3(大纲 §0.4) |
| 加速特性三分法 | §22.5 |
| 生态评估维度 | §12.7 |
| 性能对标口径 | §12.5 |
| 功率与散热约束 | §31.2 |
| 角色与协作边界 | §0.9 |

## 视觉与 Mermaid 规范

每张图必须承担定位、推导、比较、诊断、决策或证据中的至少一种功能。定量关系优先使用曲线、瀑布、热力图或工作表;不把正文列表机械转成流程图。新图使用 `<BookFigure>` 提供 alt、结论型图注、来源、许可和生成入口。

### 主题声明

每一张图的第一行必须是以下初始化块,确保全书视觉一致:

​```mermaid
%%{init: {'theme':'base','themeVariables':{
  'primaryColor':'#EEF4FF','primaryBorderColor':'#3B6FD4','primaryTextColor':'#1F2937',
  'secondaryColor':'#F3F4F6','tertiaryColor':'#FFFFFF',
  'lineColor':'#6B7280','fontFamily':'-apple-system, Segoe UI, Helvetica, Arial, sans-serif','fontSize':'14px'
}}}%%
​```

### 语义配色(全书统一,不得自创)

| 语义 | 填充 | 边框 | 用于 |
|---|---|---|---|
| 计算 / 算力 | `#EEF4FF` | `#3B6FD4` | GPU、算子、计算节点 |
| 存储 / 数据 | `#E9F7EF` | `#2E9E64` | 数据集、checkpoint、缓存 |
| 通信 / 网络 | `#FFF4E5` | `#D9822B` | 集合通信、互联、请求流 |
| 控制 / 调度 | `#F3EEFF` | `#7C5CD4` | 调度器、编排、控制面 |
| 瓶颈 / 故障 | `#FDECEC` | `#D64545` | 仅用于标注瓶颈点和故障点 |
| 外部 / 边界 | `#F3F4F6` | `#9CA3AF` | 不属于本章范围的部分 |

用 `classDef` 定义后复用,例如:
`classDef compute fill:#EEF4FF,stroke:#3B6FD4,stroke-width:1.5px,color:#1F2937`

### 硬规则

1. **单图节点数不超过 15 个**。超过就拆成两张图,或用 `subgraph` 折叠细节。
2. **必须用 `subgraph` 表达层级**,并给 subgraph 起有意义的名字(如"域内""控制面""Prefill 阶段")。
3. **跨子图、跨层或承载数据/梯度/KV/控制信号的边必须有标签**，说明流动的对象；同一子图内仅表达顺序或从属关系的短边可省略标签。标签应服务阅读,不要为了合规重复节点文字。
4. **红色只用于瓶颈和故障**,不得用作普通强调。全书读者应形成"看到红色就是问题点"的条件反射。
5. **每张图下方必须有一句图注**,格式为:`图 X-N:<标题>。<这张图要读者记住的那一件事>。` 图注不是对图的复述,而是结论。
6. 方向优先用 `LR`(横向)以适配书籍排版;层级深的用 `TB`。
7. 不使用 mermaid 的实验性图表类型(如 `journey`、`quadrantChart`),优先使用 `flowchart`、`sequenceDiagram`、`stateDiagram-v2`、`gantt`、`timeline`。
8. 中文节点文本超过 12 个字请换行(用 `<br/>`),避免节点过宽。
9. Mermaid 默认每章不超过 2 张,不承担定量曲线或默认章节结尾工件。

### 图表数量

多数章节规划 **3–5 张有效图片**,复杂度较低的章节可以更少。图片数量不是验收目标;删除后不影响理解的装饰图不进入正文。
```

---

# 第二层:逐章 Prompt

---

## 第一部分 基础与心智模型

### 第 1 章 从大数据基础设施到 AI 基础设施

```
撰写第 1 章《从大数据基础设施到 AI 基础设施》。

本章是全书的开篇,承担三件事:建立读者的认知锚点、划清能力迁移的边界、给出全书地图。

必须覆盖:
- 大数据时代基建版图的快速回顾(调度、存储、计算、流批),目的是唤起读者已有的知识框架,不要展开讲解
- 可直接复用的经验:多租户与配额模型、数据管道工程、资源隔离、可观测性方法论
- 彻底失效的经验:无状态假设、故障容忍模型(一个 task 失败重跑 vs 一次全局同步失败)、资源分配粒度(容器切分 vs 整卡独占)、弹性伸缩的前提条件
- AI Infra 的三个新约束:显存墙、通信墙、长时任务的全局同步特性
- 全书地图:七个部分的组织逻辑、两条主线的作用

写作要求:
- 问题场景请写一位大数据平台负责人接手 GPU 集群后的前两周,他基于旧经验做出的三个错误判断
- "大数据对照"这一段在本章是主体而非附属,可以占全章 30% 篇幅
- 结尾的全书地图不要写成目录复述,要写成"你的问题在哪一章"的索引

视觉规划(3 张):
1. 能力迁移图:左侧大数据基建能力,右侧 AI Infra 能力,中间用三色边区分"直接复用""需重构""完全失效"三类映射关系。用 flowchart LR + subgraph。
2. AI Infra 全景分层图:从硬件到应用的七层,标注每层对应本书哪一部分。用 flowchart TB。
3. 决策工件:"我该从哪一章开始读"——按读者的角色和当前任务分流。
```

---

### 第 2 章 两条主线解剖与框架地图

```
撰写第 2 章《两条主线解剖与框架地图》。

本章建立全书的两条贯穿线索,后续每一章都会回来引用。这是全书最需要精确的一章,因为它定义了所有后续讨论的坐标系。

必须覆盖:
- 主线 A:一次训练任务的完整链路——数据读取 → 预处理 → 前向 → 反向 → 梯度同步 → 优化器更新 → checkpoint,标注每一段的主导资源(算力/带宽/显存/IO/网络)与典型瓶颈
- 主线 B:一次推理请求的完整链路——网关 → 路由 → 排队 → Prefill → Decode 循环 → 采样后处理 → 流式返回,同样标注主导资源
- 框架三层地图:运行时/引擎层、框架/工程层、编排/平台层,以及网关层。说明为什么必须分层——读者常犯的错误是拿 vLLM 和 KServe 做对比
- 全书术语与符号约定表

写作要求:
- 两条主线的描述必须精确到"这一步在等什么",因为后面所有优化都是在消除某个等待
- 框架地图部分只讲分层逻辑与各层职责,具体项目只作为例子出现一次,不展开
- 本章的"大数据对照"可以讲一次 Spark job 的生命周期与训练任务的结构性差异

视觉规划(4 张):
1. 主线 A 训练链路图:用 flowchart LR,每个阶段节点按语义色着色,边标注传递的数据类型,瓶颈候选点用红色标注。
2. 主线 B 推理链路图:用 sequenceDiagram,展现网关、调度器、引擎、KV Cache 之间的交互时序,突出 Prefill 与 Decode 的循环关系。
3. 框架三层地图:用 flowchart TB + subgraph,三层各自列出代表项目,层间用带标签的边说明接口契约。
4. 决策工件:"我遇到的问题属于哪条主线的哪一段"。
```

---

### 第 3 章 人工智能与大模型基础

```
撰写第 3 章《人工智能与大模型基础》。

本章采用机制解释结构:每一节按"概念的直觉 → 它消耗什么资源 → 它改变什么决策"组织。

本章的定位是算法概念到资源事实的转译层,读者读完应当能听懂算法同事说的每一个词,并立刻知道那个词对应哪一笔资源开销。

必须覆盖(按此顺序):
3.1 神经网络与训练三阶段:层/参数/激活值三个词分别对应哪块显存;前向/反向/更新三阶段的资源画像;反向传播为什么必须保留前向激活值(这是激活值显存的来源,也是重计算能省显存的原因);梯度何时产生、何时同步
3.2 优化器与超参的 Infra 含义:优化器状态数量差异如何变成每参数字节数;超参三分法——影响资源的(batch size、序列长度、梯度累积)、影响收敛的(学习率、warmup)、两者都影响的(并行配置)
3.3 从 NLP 到 LLM:下一个 token 预测(一句话讲完);token 作为吞吐/计费/限流/上下文长度的统一单位;词表大小的资源代价
3.4 Transformer 与注意力:Q/K/V 与 head 的形状(只讲形状不讲推导);注意力开销随序列长度的增长关系;KV Cache 的由来——自回归解码为什么必须缓存
3.5 模型谱系:Dense 与 MoE 的稀疏激活直觉、总参数与激活参数为什么必须分开看;多模态三段式构成;规模量级感
3.6 训练范式全景:Pretrain/CPT/SFT/RLHF-RL 四阶段各自解决什么问题、数据量级差多少;PEFT 与 LoRA 的直觉及其在部署侧带来的多租户可能
3.7 评测与 Benchmark 的基本概念:精度评测与性能评测是两套完全不同的东西;Infra 侧为什么必须关心评测
3.8 推荐读物

两条硬约束:
1. 每一节必须以"它消耗什么资源 / 它改变什么决策"收尾,写不出这句话的内容整节删掉
2. 全章不出现任何公式推导,只出现形状、量级、比例关系

视觉规划(4 张):
1. 训练三阶段资源画像图:前向/反向/更新三阶段,标注各阶段的算力、显存、通信特征,突出"反向阶段显存峰值"这个事实。用 flowchart LR。
2. 显存构成来源图:参数、梯度、优化器状态、激活值、KV Cache 五类显存分别从哪个环节产生。用 flowchart TB。
3. 训练范式全景图:Pretrain → CPT → SFT → RL 的关系与各自数据量级,用 timeline 或 flowchart LR。
4. 术语到资源的映射图:把本章所有算法术语映射到它影响的资源维度,作为全章总结。
```

---

### 第 4 章 硬件第一性原理、架构范式与数值精度

```
撰写第 4 章《硬件第一性原理、架构范式与数值精度》。

本章是全书的物理地基,后面每一次性能估算都要回到这里。

必须覆盖:
4.1 四个数字与物理约束
- 算力、显存容量、显存带宽、互联带宽;显存层级及各级带宽容量差
- HBM:堆叠结构与 2.5D 封装、为什么不能像 DDR 那样扩容;容量与带宽的代际演进为什么慢于算力;HBM 作为全行业最紧供应约束的含义;标称容量 vs 实际可用(ECC、驱动预留、碎片)
- 规模层级:单卡 → 单机 → 超节点(统一互联域)→ 多机集群。关键抽象是"互联域"而非某厂商产品形态;它改写了哪些经典经验
- 功耗与散热:只埋一句伏笔,完整讨论在第 31 章

4.2 加速器架构范式
- 四类范式(SIMT 通用并行 / 脉动阵列 / 多单元协同 / 推理专用 ASIC)及其生态成熟度
- 核心洞察:四类范式的真正差异不在峰值算力,而在编译器承担多少责任
- 训练型与推理型负载的硬件诉求差异:六维配比表。明确声明本书不采用"训练卡/推理卡"二分法及理由
- 界限模糊化:RL 训练把 rollout 拉进训练流程,训练集群必须跑得动推理引擎
- 如何读一张卡的 spec sheet:该看的六个数字、哪些是营销数字

4.3 数值格式与精度【全书唯一定义处】
- 位宽的分配逻辑、动态范围与表示精度是两件事
- 格式谱系 FP32/TF32/FP16/BF16/FP8(E4M3、E5M2)/FP4/INT8/INT4
- 为什么 BF16 取代 FP16——赢在动态范围
- 精度-算力倍率关系
- 记法约定 W8A8 / W4A16 / W4A8KV4
- 累加精度与计算精度的分离
- INT32 作为累加器与索引类型的位置

4.4 Roofline 模型:计算密度、拐点、把精度代入后同一算子如何跨越拐点

写作要求:
- 问题场景请写一次采购决策:两张卡参数各有胜负,团队争论不下,最后发现真正决定的是负载画像而非峰值算力
- 精度那一节要有一张完整的格式对照表
- 强调本章的方法论价值:读者面对任何一张未来才出现的卡,都应该能自己判断

视觉规划(4 张):
1. 规模层级图:单卡 → 单机 → 超节点(互联域)→ 多机集群四级,标注各级带宽量级与断崖位置。用 flowchart TB + subgraph,域边界用醒目样式。
2. Roofline 示意图:用 flowchart 或 xychart 表达计算密度与性能上限的关系,标出 Attention 与 FFN 的典型位置,以及降精度后位置的移动。
3. 精度谱系图:各格式按位宽排列,标注每元素字节数与相对算力倍率。用 flowchart LR。
4. 决策工件:"给定一个负载画像,我该看这张卡的哪几个数字"。
```

---

### 第 5 章 CUDA、CANN 与加速器软件栈

```
撰写第 5 章《CUDA、CANN 与加速器软件栈》。

本章补上硬件与集群之间断掉的一层。明确边界:不教写 kernel,只讲到能读懂 profile、能排查版本冲突为止。

必须覆盖:
5.1 CUDA 执行模型与软件栈:SM/warp/thread block/共享内存与寄存器/occupancy;driver/runtime/toolkit 版本兼容矩阵;cuBLAS、cuDNN、NCCL、CUTLASS 的位置;容器里的 CUDA 与版本冲突排查路径;Stream 与 CUDA Graph——为什么小 batch 的 decode 阶段必须靠 Graph 消除 launch 开销

5.2 运行时的共性问题:PyTorch caching allocator 的行为与 OOM 的真实成因;INT32 索引在大 tensor/长序列/大词表下的静默溢出(生产环境最难查的一类 bug);PyTorch 作为共同底座的角色

5.3 CANN 与昇腾软件栈:driver/CANN/框架适配层 与 driver/CUDA/PyTorch 的完整栈对照;AscendCL、图引擎、Ascend C 算子开发、ATC 模型转换各自对应 CUDA 世界的什么;图编译模式 vs 单算子下发,ACL Graph 与 CUDA Graph 的对应关系;昇腾侧显存模型与 OOM 成因差异。核心立场:这不是 CUDA 的一个方言,是两套完整并行的世界

5.4 Profiling 方法论:如何读时间线定位 bubble;昇腾侧工具与指标的对应;ROCm 生态简述

写作要求:
- 问题场景请写一次"训练能跑但只有预期一半速度"的排查过程,最终定位到 launch 开销
- 版本冲突那一段要给出可操作的排查顺序,而不是罗列可能原因
- 本章的"大数据对照"可以讲 JVM 调优与显存分配器调优在思维方式上的相似与不同

视觉规划(4 张):
1. CUDA 与 CANN 双栈对照图:左右两列分层对照,同层之间用虚线连接表示"对应关系"。用 flowchart TB + 两个 subgraph。这是本章的核心图,也是全书被引用最多的图之一。
2. profile 时间线示意图:用 gantt 表达一次训练迭代中各阶段的时间占用,标出 bubble 的三种典型形态。
3. OOM 成因归因树:从"显存不足"这个现象出发,分叉到碎片、峰值、泄漏、预留四类原因及各自的判定方法。用 flowchart TB。
4. 决策工件:"我的性能问题该用哪个工具、看哪个指标"。
```

---

### 第 6 章 模型结构的定量分析

```
撰写第 6 章《模型结构的定量分析》。

本章是第五、六部分的入场券。第 3 章讲清了"是什么、为什么吃资源",本章讲"算多少"。

【与第 3 章的分工】同一概念在两章出现不算重复:第 3 章给直觉,本章给公式。写作时请明确引用第 3 章的对应小节,不要重述直觉部分。

必须覆盖:
6.1 算子与瓶颈归属:Transformer block 的算子构成与形状参数;Attention 是 memory-bound、FFN 是 compute-bound 及其推导思路;Prefill 与 Decode 两阶段的定量资源画像差异
6.2 参数量与资源换算【核心节】:参数量到算力的换算(训练约 6N FLOPs/token、推理约 2N)及反推训练时长与卡数的方法;参数量到显存的换算(权重、梯度、优化器状态的每参数字节数);KV Cache 随 batch × seqlen 的线性增长;稠密与 MoE 的总参数/激活参数分离——显存按总参数算、算力按激活参数算,这是 Infra 侧最容易算错的一处;Scaling 与算力预算;规模谱系表(每档规模对应的部署形态);权重格式与分发
6.3 结构变体的定量代价:MoE 专家负载不均的量化刻画与 All2All 通信量;长上下文的 KV Cache 线性代价与显存墙触碰点;大词表的 embedding 与 lm_head 显存占比
6.4 多模态的定量画像:三段式各自的算力显存占比;token 膨胀系数
6.5 训练范式的资源画像量化

写作要求:
- 本章必须有完整的算例:选一个具体规模的模型,从头算一遍显存、算力、通信量,让读者能照着套
- 每个公式都要说明它的适用条件和误差来源
- 规模谱系表是全书被翻得最勤的一页,要做得足够实用

视觉规划(3 张):
1. 显存三分构成图:参数、梯度、优化器状态、激活值、KV Cache 的相对占比随训练/推理场景的变化。用 flowchart LR 或 xychart-beta。
2. 从参数量到卡数的推导链:参数量 → 算力需求 → 显存需求 → 并行策略 → 卡数,每一步标注所用公式。用 flowchart LR,这是本章的方法论主图。
3. 决策工件:"给定模型和 SLO,我需要多少卡"。
```

---

### 第 7 章 互联与集合通信

```
撰写第 7 章《互联与集合通信》。

本章定义"互联域"这个抽象,它是全书后半部分反复出现的一条线。

必须覆盖:
- 卡间与机间的互联技术:NVLink/NVSwitch、PCIe、RDMA/RoCE/InfiniBand,只讲带宽量级与拓扑特性,不讲协议细节
- 集合通信原语:AllReduce、AllGather、ReduceScatter、All2All,说明各自的通信量与在训练中出现的位置(All2All 是 MoE 的关键)
- 拓扑感知:Ring 与 Tree 算法的适用场景、NCCL 的拓扑探测与调优入口
- 互联域与三级拓扑【本章核心】:超节点把拓扑从"域内/跨机"两级变成"卡间/域内/跨域"三级;带宽在域边界上的断崖式下跌;通信算法与并行切分必须对齐域边界
- HCCL 与昇腾互联:与 NCCL 的原语对应关系、HCCS 拓扑、组网差异与调优入口
- 通信量估算:各并行策略下的每步通信量公式,区分域内与跨域两套带宽参数
- 低精度通信与梯度压缩:通信量随精度线性缩减、压缩的收敛代价与适用边界
- 网络故障的表现形式与排查

写作要求:
- 问题场景请写一次"加卡不加速"——扩大集群规模后吞吐反而下降,定位到跨域通信
- "域边界"这个概念要讲得足够醒目,因为第 16、20、24、31 章都会回来引用
- 本章的"大数据对照"讲 shuffle 与集合通信的异同:都是全局数据交换,但一个可以落盘重试,一个是同步阻塞的

视觉规划(4 张):
1. 三级拓扑图:卡间/域内/跨域三层,标注各层带宽量级,域边界用醒目样式突出断崖。用 flowchart TB + subgraph。本章核心图。
2. 集合通信原语示意图:AllReduce 的 Ring 实现过程,用 sequenceDiagram 或 flowchart 表达数据流转。
3. 并行策略与通信模式对应图:DP/TP/PP/EP 各自触发哪种原语、通信量与什么成正比。用 flowchart LR。
4. 决策工件:"我的通信瓶颈在哪一级,该调什么"。
```

---

## 第二部分 算力底座

### 第 8 章 K8s 上的加速卡

```
撰写第 8 章《K8s 上的加速卡》。

本章开始进入资源层,读者的 K8s 背景在这里第一次派上用场,也第一次遇到它的失效边界。

必须覆盖:
- 设备插件机制与 GPU Operator 的职责边界
- 资源模型:为什么加速卡不是 CPU——不可超卖、不可细分(默认)、拓扑敏感;整卡分配的代价
- 拓扑感知调度:NUMA 亲和、卡间互联亲和、网卡亲和;三者冲突时如何取舍
- Ascend Device Plugin 与 NPU 调度:资源上报、拓扑感知、与 GPU 路径的配置差异
- 节点生命周期:驱动升级的滚动策略、健康检查该检什么、故障节点自动隔离的判定条件与误判代价

写作要求:
- 问题场景请写一个"调度成功但性能腰斩"的案例:Pod 拿到了 8 张卡,但跨了 NUMA 和互联域
- "大数据对照"讲 YARN 的资源模型为什么在这里不够用:资源维度从二维(CPU/内存)变成带拓扑约束的多维
- 强调一个反直觉的事实:在 AI 集群里,调度器把任务放错位置的代价,比把任务排队等待的代价大得多

视觉规划(3 张):
1. 设备插件与调度链路图:kubelet、device plugin、scheduler、GPU Operator 之间的交互与资源上报路径。用 sequenceDiagram。
2. 拓扑亲和示意图:一台八卡机器的 NUMA、PCIe、互联拓扑,标出"好的分配"与"坏的分配"两种情况。用 flowchart TB + subgraph,坏分配用红色标注。
3. 决策工件:"我的集群该配哪些调度约束"。
```

---

### 第 9 章 批调度器

```
撰写第 9 章《批调度器》。

必须覆盖:
- 为什么默认调度器不够用:Gang Scheduling 的必要性、部分调度导致的资源死锁与资源空耗
- 队列、配额、优先级、抢占四件套的模型设计
- Volcano / Kueue / Slurm 的模型差异:各自的抽象是什么、适合什么规模与组织形态、失效边界在哪
- 训练任务与推理服务混部:潮汐利用、抢占对在线服务的影响、混部的收益边界
- 排队策略:FIFO、公平共享、backfill 在 AI 场景下的表现差异

写作要求:
- 问题场景请写资源死锁:两个八卡任务各拿到四张卡,互相等待,集群利用率显示 100% 但没有任何进展
- 三个调度器的对比必须写明失效边界,不能写成功能对照表
- "大数据对照"讲 YARN 队列模型与 Gang Scheduling 的本质差异:批处理容忍部分启动,同步训练不容忍

视觉规划(3 张):
1. 资源死锁形成过程:用 sequenceDiagram 展现两个任务逐步占用资源直至互锁的时序。
2. 调度器决策流程图:一个任务从提交到运行经历的判定链路(配额检查 → 队列排序 → Gang 检查 → 拓扑匹配 → 绑定)。用 flowchart TB。
3. 决策工件:"我的组织形态与集群规模该选哪个调度器"。
```

---

### 第 10 章 异构与池化

```
撰写第 10 章《异构与池化》。

本章篇幅可以相对紧凑,它服务于一个具体决策:要不要切分或池化算力。

必须覆盖:
- MIG、时分复用、vGPU 三种切分方式的原理、隔离强度与性能代价
- 各自的适用边界:什么负载适合切分,什么负载切了就废
- 算力池化与远程调用:收益模型与延迟代价,什么规模下才划算
- 多代硬件共存:调度约束、性能对齐问题、如何避免木桶效应

写作要求:
- 问题场景请写一个推理服务切分 GPU 后延迟长尾恶化的案例
- 明确给出一条判断:大模型训练不要切分;小模型推理和开发环境才是切分的主战场
- "大数据对照"讲容器超卖在 CPU 世界的成功与在显存世界的失败

视觉规划(2 张):
1. 三种切分方式的隔离层次对比:从硬件级到时间片级,标注各自的隔离强度与开销。用 flowchart TB。
2. 决策工件:"我的负载该不该切分,切到什么粒度"。
```

---

### 第 11 章 多租户与利用率治理

```
撰写第 11 章《多租户与利用率治理》。

本章解决一个所有平台负责人都会被问到的问题:利用率为什么这么低,怎么提上去。

必须覆盖:
- 配额模型:静态配额、弹性配额、借用与归还机制的设计与副作用
- MFU 与真实利用率的差距【本章核心】:为什么 nvidia-smi 显示的利用率是个误导性指标;它统计的是什么、不统计什么;MFU 的正确算法
- 利用率归因:数据等待、通信等待、调度空窗、代码低效四类原因的识别方法与典型占比
- 治理手段:任务画像、闲置回收、超卖策略、以及每种手段的组织成本
- 一个常被忽略的问题:利用率优化的边际收益递减,以及什么时候该停

写作要求:
- 问题场景请写一次向管理层汇报:报表显示利用率 85%,但实际有效算力只有 30%
- 归因方法要可操作,给出具体的观测指标与判定阈值思路
- "大数据对照"讲 Hadoop 时代的资源利用率治理经验哪些可以直接搬过来

视觉规划(3 张):
1. 利用率归因树:从"卡时被占用但没产出"出发,分叉到四类原因及各自的观测手段。用 flowchart TB。这是本章核心图。
2. 三种利用率指标对比图:占用率、SM 利用率、MFU 分别统计了什么,用同一时间轴上的分层示意。用 gantt 或 flowchart LR。
3. 决策工件:"我的低利用率属于哪一类,该用哪种治理手段"。
```

---

### 第 12 章 国产算力平台与异构迁移

```
撰写第 12 章《国产算力平台与异构迁移》。

这是中文技术书里几乎空白的一章,也是本书差异化最强的部分之一。前面各章已经按层插入了昇腾的技术对照,本章集中解决决策与迁移问题,不要重复讲技术栈细节(引用第 5 章)。

必须覆盖:
12.1 国产算力版图:各平台的定位差异与生态成熟度(只讲定位,不讲参数);训练侧与推理侧成熟度不对称——为什么推理先落地、训练更难
12.2 迁移的三道坎:
- 第一道 算子覆盖率:如何快速摸清模型用到的算子有多少已支持;缺失算子的三条出路(等待、替换、自研)及各自成本
- 第二道 精度对齐:逐层比对方法、累积误差定位、什么样的差异可以接受
- 第三道 性能调优:算子融合策略差异、内存搬运模式差异、通信参数重调
- 核心结论:90% 的项目卡在第三道——能跑通不等于能用
12.3 迁移评估方法论:如何在两周内判断能不能迁、性能损失多少、需要投入多少人力;什么样的模型不该迁
12.4 混合集群管理:调度、镜像、监控的统一;一套代码两套后端的抽象层该放在哪一层;CI 如何覆盖双后端
12.5 性能对标的正确姿势【全书唯一定义处】:为什么单卡跑分没有意义;要看端到端 MFU、集群规模效率、每元算力的有效吞吐;对标实验的设计与常见的不诚实比法
12.6 供应链与生命周期视角
12.7 生态作为选型变量【全书唯一定义处】:五个可评估维度(算子模型覆盖率与更新滞后期、框架适配深度、文档与社区可得性、人才供给、迭代与兼容记录);硬观点——换硬件的真实成本里硬件差价往往不是大头,生态成熟度折算的工时才是;如何把生态折成人月进 TCO 模型

写作要求:
- 问题场景请写一次迁移项目:三个月跑通,又花六个月才把性能追到可接受水平
- 本章要有明确立场,但不能变成对任何平台的褒贬,立场必须落在方法论上
- 生态那一节的量化方法是本章最有价值的部分,要写透

视觉规划(4 张):
1. 迁移三道坎示意图:算子覆盖 → 精度对齐 → 性能调优三阶段,标注各阶段的典型耗时占比与卡住比例。用 flowchart LR,第三道坎用红色突出。
2. 迁移评估流程图:两周评估的具体步骤与每步的判定输出。用 flowchart TB。
3. 生态成熟度雷达维度图:五个评估维度及其折算为人月的路径。用 flowchart LR。
4. 决策工件:"我的业务该不该迁、迁哪一部分"。
```

---

## 第三部分 数据底座

### 第 13 章 训练数据管道

```
撰写第 13 章《训练数据管道》。

这是全书大数据经验密度最高的一章,读者的既有能力在这里最能发挥。

必须覆盖:
- 采集、清洗、精确去重与近似去重、质量过滤的工程实现与规模瓶颈
- 数据配比、课程学习、采样策略的工程实现(只讲怎么实现,不讲怎么定配比)
- 合成数据的生产流水线与污染防控
- 数据版本与可复现性:数据集也需要 commit 的概念
- 非文本数据管道:体量比纯文本高两三个数量级;瓶颈从 IO 转到 CPU 解码;NVDEC 硬解、DALI、WebDataset 分片格式;预处理与训练的流水线重叠
- 数据飞轮【本章新增重点】:线上请求回流 → 筛选与脱敏 → 标注 → 进训练集 → 模型更新 → 再回流。重点写回流采样率与存储成本、隐私边界与合规、污染防控(线上数据可能含模型自己的输出,自噬问题的工程防线)、与第 29 章质量监控和第 30 章生命周期的接口

写作要求:
- 问题场景请写 GPU 等数据:昂贵的卡有 40% 时间在等数据管道
- 数据飞轮那一节要把它讲成基础设施问题而非算法问题
- "大数据对照"在本章可以放开写:Spark 的去重、Flink 的流式清洗、数据湖的版本管理,哪些直接搬、哪些量级不够

视觉规划(4 张):
1. 训练数据管道全景图:从原始数据到训练样本的完整链路,标注每段的瓶颈类型。用 flowchart LR。
2. 流水线重叠示意图:数据预处理与训练计算的时间重叠关系,对比"串行"与"重叠"两种模式的时间轴。用 gantt。
3. 数据飞轮闭环图:线上服务 → 回流 → 筛选脱敏 → 标注 → 训练 → 上线的闭环,标注每一环的合规卡点与污染风险点。用 flowchart LR 环形结构。
4. 决策工件:"我的数据瓶颈在哪一段"。
```

---

### 第 14 章 存储与 Checkpoint I/O

```
撰写第 14 章《存储与 Checkpoint I/O》。

必须覆盖:
- 三层存储的分工:对象存储、并行文件系统、本地 NVMe,各自承载什么、成本与性能的取舍
- 缓存加速层:Alluxio/JuiceFS 类方案的适用场景与失效边界
- Checkpoint 的突发写模式【本章核心】:为什么它和常规负载完全不同——瞬时全集群写入、写完即闲、大小可预测但峰值极高;如何按集群规模估算写入窗口
- 异步 checkpoint、分片保存、增量保存三种优化的原理与代价
- 小文件问题在 AI 场景的重演形式:数据集的海量小样本、以及它与 HDFS 时代的异同

写作要求:
- 问题场景请写 checkpoint 把存储打满导致整个集群卡住的事故
- 存储选型要给出按集群规模的分档建议
- "大数据对照"讲 HDFS 小文件问题的重演,以及为什么老办法(合并小文件)在这里只解决一半

视觉规划(3 张):
1. 三层存储分工图:各层的容量、带宽、成本量级与承载的数据类型。用 flowchart TB + subgraph。
2. Checkpoint 写入时序图:同步保存 vs 异步保存两种模式下,训练计算与存储写入的时间关系对比。用 gantt。
3. 决策工件:"我的集群规模与 checkpoint 频率该选哪种存储方案"。
```

---

### 第 15 章 向量与检索基础设施

```
撰写第 15 章《向量与检索基础设施》。

本章篇幅可以相对紧凑,聚焦基础设施视角,不讲检索效果优化。

必须覆盖:
- 索引结构:HNSW、IVF、量化索引的原理概要与资源画像(内存占用、构建成本、查询延迟)
- 增量更新、删除、一致性:向量库最容易被低估的工程难点
- 混合检索与召回链路的工程实现
- 规模与成本:内存占用估算方法、分片与副本策略

写作要求:
- 问题场景请写一个 RAG 系统在数据量增长十倍后的崩溃过程
- 索引选型要给出按数据规模与召回要求的分档建议
- "大数据对照"讲 Elasticsearch 的分片与副本经验在向量场景的适用与不适用

视觉规划(2 张):
1. 索引结构资源画像对比图:三类索引在内存、构建时间、查询延迟、召回率四个维度的相对位置。用 flowchart LR。
2. 决策工件:"我的数据规模与召回要求该选哪种索引"。
```

---

## 第四部分 训练系统

### 第 16 章 并行策略

```
撰写第 16 章《并行策略》。

这是训练部分的主干章节,难度也最高。

必须覆盖:
- DP/TP/PP/EP/SP 各自的切分维度与原理(引用第 6 章的形状分析,不重述)
- 各策略的通信量与显存收益公式:必须可计算
- 3D/4D 并行的组合选型与搜索空间:先切什么、后切什么的经验顺序及其依据
- 超节点如何改写并行选型【重点】:TP 域可扩到远超 8 卡,"TP 不跨机"这条经典经验直接作废;MoE 的 All2All 从跨机瓶颈变为域内操作,EP 可行规模大幅上移;并行切分与域边界的对齐规则
- 流水线气泡与调度算法:气泡的量化计算与主流调度方案的对比
- 变长与异构负载下的并行:padding 浪费与 sequence packing;encoder 与 decoder 采用不同并行策略的组合问题(引用 §4.2.1 的"编译器责任"结论)
- 并行原语在各框架中的 API 映射:只给映射关系,不写具体 API

写作要求:
- 问题场景请写一次并行策略选错:同样的卡数,吞吐差三倍
- 必须给出一个完整的选型算例
- "大数据对照"讲数据并行与 MapReduce 的相似性,以及模型并行为什么在大数据时代没有对应物

视觉规划(4 张):
1. 五种并行策略的切分维度示意图:同一个模型分别按 DP/TP/PP/EP/SP 切分的对比。用 flowchart LR + subgraph,每种策略一个 subgraph。
2. 流水线气泡示意图:朴素流水线与优化调度的时间轴对比,气泡区域用红色标注。用 gantt。
3. 并行切分与域边界对齐图:好的对齐与坏的对齐两种情况,标注跨域通信量差异。用 flowchart TB。
4. 决策工件:"给定模型规模、卡数、互联拓扑,我该怎么切"。这是全书最重要的决策树之一,要做得足够细。
```

---

### 第 17 章 显存与训练侧精度机制

```
撰写第 17 章《显存与训练侧精度机制》。

【边界】数值格式的定义已在 §4.3 完成,本章只讲训练侧的精度机制,不重述格式谱系。

必须覆盖:
- ZeRO 三个阶段的原理与代价(分片对象引用 §3.2 优化器状态)
- 激活重计算的粒度选择:全量重算、选择性重算、按层重算的收益与代价对比(原理引用 §3.1 反向传播)
- CPU/NVMe Offload 的收益边界:什么情况下 offload 是救命稻草,什么情况下是自欺欺人
- 混合精度训练机制:master weights、loss scaling、per-tensor 与 per-block scaling 策略
- FP8 训练的工程门槛:哪些层能降、哪些必须留高精度、scaling 因子的更新策略
- 数值发散的排查路径:loss spike、NaN 溯源、精度回退的诊断顺序
- 显存 OOM 的系统性排查路径(引用 §5.2 分配器)

写作要求:
- 问题场景请写一次 FP8 训练在第 3000 步突然 NaN 的排查
- 各项显存优化要给出"省多少显存、付出多少吞吐代价"的量化对照
- "大数据对照"讲内存溢出排查方法论的迁移

视觉规划(3 张):
1. 显存优化技术的收益代价矩阵:各技术在"省显存量"与"吞吐损失"两个维度的位置。用 flowchart LR 或表格式布局。
2. 数值发散诊断流程图:从 loss 异常出发的分诊路径。用 flowchart TB。
3. 决策工件:"我的显存不够,该按什么顺序上哪些手段"。
```

---

### 第 18 章 并行运行时剖析

```
撰写第 18 章《并行运行时剖析》。

【纪律】本章讲的是机制而非产品。描述维度严格固定为:调度策略、显存管理、并行支持、扩展点、失效边界。不写版本号、不写 API 签名、不写安装配置。

必须覆盖:
- Megatron-Core、DeepSpeed、FSDP2 三者的机制差异,按上述五个维度逐一对照
- 组合使用的边界与常见冲突:哪些能叠、哪些互斥、叠加后谁管显存
- 昇腾训练路径:MindSpeed/ModelLink 的定位;PyTorch + torch_npu 与 MindSpore 两条路线的取舍与迁移成本
- 何时该自己写训练循环,何时不该:一条明确的判断标准

写作要求:
- 问题场景请写一次框架组合踩坑:两个显存优化叠加后反而 OOM
- 三者对比必须写出各自的失效边界,不能写成功能矩阵
- "大数据对照"讲计算引擎选型的方法论迁移:当年怎么选 MapReduce/Spark/Flink,今天怎么选训练运行时

视觉规划(3 张):
1. 三个运行时的五维机制对照图:用 flowchart 表达各自在五个维度上的设计取舍。
2. 显存管理职责边界图:框架、运行时、分配器三层各自管什么,冲突发生在哪。用 flowchart TB + subgraph。
3. 决策工件:"我的场景该选哪个运行时,能不能组合"。
```

---

### 第 19 章 训练工程层与编排

```
撰写第 19 章《训练工程层与编排》。

必须覆盖:
19.1 工程层:TorchTitan、LLaMA-Factory/Axolotl 的定位与适用规模
19.2 编排层:Ray Train、Slurm、Kubeflow 的分工与选型
19.3 三层接口契约【本章核心】:谁负责进程组、谁负责重启、谁负责数据分发——职责不清是这一层所有问题的根源
19.4 实验管理与超参搜索的基础设施支撑
19.5 微调即服务的平台形态【本节是本章最有价值的部分】:
- 调度诉求相反:小任务多、突发强、生命周期短,与大规模预训练的长时独占刚好对立
- 数据隔离与权限:存储、镜像、日志三条路径都要隔离
- 模板化:把并行策略、超参默认值、显存配置封装成"选个基座 + 传个数据集就能跑"
- 产出物管理:几百个 LoRA 权重的注册、评测、上线流转
- 成本归属:按 token 还是按卡时计费,两种口径对业务方行为的不同引导
- 失败任务的自助诊断

写作要求:
- 问题场景请写平台方被业务方的微调任务淹没:队列排满、故障工单堆积、每个都要平台介入
- §19.5 要写成可落地的平台设计,而不是功能罗列
- "大数据对照"讲自助式数据平台的建设经验如何迁移

视觉规划(4 张):
1. 三层职责边界图:编排层、工程层、引擎层各自管什么,接口在哪。用 flowchart TB + subgraph。
2. 一次训练任务的重启责任链:故障发生后,哪一层先感知、哪一层负责恢复。用 sequenceDiagram。
3. 微调即服务的平台架构图:从业务方提交到产出物上线的完整链路,标注隔离边界与卡点。用 flowchart LR。
4. 决策工件:"我的平台该提供到哪一层的抽象"。
```

---

### 第 20 章 稳定性与容错

```
撰写第 20 章《稳定性与容错》。

必须覆盖:
- 长时任务的失败率模型:为什么千卡规模下故障是常态——给出可计算的估算方法
- 故障检测:hang 检测、集合通信超时、静默错误三类问题的识别难度递增及各自手段
- 慢节点识别与踢除:慢节点比故障节点更难处理,因为它不报错
- 故障域的放大【重点】:超节点下一次域内故障的影响面从 8 卡变为上百卡;失败率模型、重调度粒度、备机策略都要重算
- 弹性训练与断点续训:实现代价与收益边界
- 低精度下的可复现性:归约顺序的非确定性、变卡数续训的结果漂移、什么情况下必须追求 bitwise 一致
- 故障演练与 SLO:有效训练时间占比的定义与目标设定

写作要求:
- 问题场景请写一次千卡训练:两周内重启十七次,最终有效训练时间只有 60%
- 失败率模型要给出可算的公式,让读者能估自己集群的预期中断频率
- "大数据对照"讲 task 级重试与全局同步任务的容错哲学差异——这是本书最重要的一处对照

视觉规划(4 张):
1. 三类故障的检测难度谱系:崩溃、hang、静默错误,标注各自的检测手段与平均发现时长。用 flowchart LR。
2. 故障域放大示意图:传统单机故障域与超节点域故障的影响面对比。用 flowchart TB + subgraph,影响范围用红色标注。
3. 断点续训时序图:故障发生到恢复训练的完整时序,标注各阶段耗时构成。用 sequenceDiagram 或 gantt。
4. 决策工件:"我的集群规模该投入多少容错能力"。
```

---

### 第 21 章 后训练基础设施

```
撰写第 21 章《后训练基础设施》。

这是当下资料最稀缺、也最值得写深的一章,请给予最高的信息密度。

必须覆盖:
- SFT/LoRA/DPO/RLHF 各自的资源画像(概念引用 §3.6,定量引用 §6.5,本章只讲基础设施差异)
- RL 训练框架专节:verl、OpenRLHF、TRL、slime 等。重点不是 API,而是它们如何解决 rollout 与 train 的资源潮汐——这是 RL Infra 的核心矛盾
- 混合架构:训练实例与推理实例共存的三种模式(分离部署、共卡时分、权重原地切换);权重同步的代价与实现;显存倒换
- 奖励模型与环境服务的基础设施:它们是被忽略的一大块资源开销
- 与 §4.2.2 的呼应:RL 让"训练卡与推理卡"的界限彻底失效

写作要求:
- 问题场景请写 RL 训练的资源浪费:rollout 阶段训练卡全闲,train 阶段推理卡全闲
- 资源潮汐是本章的主线,所有方案都要放在这个坐标下评价
- "大数据对照"可以讲流批一体的架构演进与这里的相似困境

视觉规划(4 张):
1. RL 训练的资源潮汐图:一个迭代周期内 rollout 与 train 两阶段的资源占用曲线,突出空闲区域。用 gantt 或 xychart-beta。
2. 三种混合架构对比图:分离部署、共卡时分、权重原地切换的资源布局对比。用 flowchart TB + subgraph。
3. 一次 RL 迭代的完整时序:actor、critic、reward model、环境服务之间的交互与权重同步时点。用 sequenceDiagram。
4. 决策工件:"我的 RL 训练规模该选哪种混合架构"。
```

---

## 第五部分 推理系统

### 第 22 章 推理引擎原理

```
撰写第 22 章《推理引擎原理》。

本章是推理部分的地基,同时承载全书的"加速特性三分法"这一组织框架。

必须覆盖:
22.1 KV Cache 与显存:结构、增长规律、显存占比(由来见 §3.4,不重述);KV Cache 精度的系统影响——降精度如何同时改变显存占用、可容纳并发数与前缀缓存命中率;PagedAttention 与显存碎片治理
22.2 批处理与调度:Continuous Batching 的原理与调度器设计;Chunked Prefill——长 prompt 把 decode 请求饿死是所有推理服务都会遇到的问题,写清分块原理、块大小选择、以及它对 TTFT 与 TPOT 的权衡影响;优先级、抢占与请求驱逐
22.3 采样与后处理阶段:贪婪采样、Top-k/Top-p、温度、repetition penalty 各自在做什么;Infra 侧的关键事实——大词表下 sampling 是 memory-bound 的,logits 张量本身很大,后处理在 decode 阶段的耗时占比常被严重低估;结构化输出与约束解码引入的 CPU 侧串行;stop 条件、流式输出与首 token 返回的工程细节
22.4 引擎剖析:vLLM、SGLang、TensorRT-LLM、LMDeploy 的调度器与内存管理差异;昇腾推理路径 MindIE 与 vLLM-Ascend 的现状与选型;边缘侧 llama.cpp/Ollama 的不同取舍
22.5 加速特性三分法【全书唯一定义处】:调度类(优化吞吐与排队延迟)、显存类(优化并发上限)、计算类(优化单步耗时)。读者拿到任何一个新特性都应能立刻归类,并知道它和哪些特性冲突——调度类与显存类经常互相挤兑

写作要求:
- 问题场景请写一个推理服务的 P99 延迟毛刺:定位到长 prompt 阻塞
- 三分法要放在本章末尾作为整个第五部分的地图,写得足够清晰以便后两章引用
- 采样那一节要打破读者的直觉:很多人以为后处理不占时间

视觉规划(4 张):
1. 一次推理请求的引擎内部时序:排队 → Prefill → Decode 循环 → 采样 → 返回,标注各阶段的资源类型。用 sequenceDiagram。
2. Chunked Prefill 效果对比图:不分块与分块两种情况下,长 prompt 对 decode 请求的阻塞情况对比。用 gantt,阻塞区域用红色。
3. 加速特性三分法地图:三类特性及其代表手段、优化目标、冲突关系。用 flowchart TB + subgraph。这是第五部分的核心图。
4. 决策工件:"我的性能问题属于三类中的哪一类"。
```

---

### 第 23 章 推理优化:计算类加速

```
撰写第 23 章《推理优化:计算类加速》。

【边界】格式与记法已在 §4.3 定义,本章只讲推理侧的量化算法与计算加速,不重述格式谱系。本章对应三分法中的"计算类"。

必须覆盖:
23.1 量化:PTQ 与 QAT 的适用场景;逐通道/逐组量化;离群值处理;W/A/KV 三者分别降精度的收益差异与组合选型;量化工具链的定位差异;精度评估——如何验证量化后没有掉点、评测集的选择陷阱
23.2 算子与融合:编译与算子层的分工(Triton 语言、TorchInductor、FlashAttention/FlashInfer、CUTLASS);昇腾侧融合特性(VF 融合、CF 融合等)的适用条件与收益,以及与 GPU 侧融合思路的异同;自定义算子的开发与维护成本
23.3 图模式:CUDA Graph 与 ACL Graph 消除 launch 开销的原理与限制(基础见 §5.1、§5.3);动态形状与图模式的天然冲突及工程折中
23.4 投机推理:草稿模型、Medusa 类方法;接受率与收益模型——什么接受率以下不划算;与调度类特性的相互影响
23.5 叠加与冲突:各项优化的叠加效应与冲突矩阵;优化顺序的建议路径

写作要求:
- 问题场景请写一次优化叠加失败:量化 + 图模式 + 投机解码三者同时开启后性能反而下降
- 每项优化都要给出"收益区间"与"失效条件"
- 冲突矩阵是本章最实用的产出

视觉规划(4 张):
1. 计算类优化的收益归因图:单步耗时的构成(launch 开销、访存、计算),以及各项优化分别削减哪一块。用 flowchart LR。
2. 投机推理收益模型图:接受率与实际加速比的关系曲线,标出盈亏平衡点。用 xychart-beta 或 flowchart。
3. 优化叠加冲突矩阵:各项优化两两之间是增益、中性还是冲突。用 flowchart 表格式布局。
4. 决策工件:"我该按什么顺序上哪些计算类优化"。
```

---

### 第 24 章 服务化架构与显存类加速

```
撰写第 24 章《服务化架构与显存类加速》。

本章对应三分法中的"显存类",同时承载推理侧的架构设计。

必须覆盖:
- PD 分离:动机、KV 传输代价、适用边界——什么规模什么负载下才划算
- 域内 PD 分离【重点】:超节点下 KV 传输代价急剧下降,分离的收益模型随之改变;跨域分离为什么依然不划算(引用第 7 章域边界)
- Prefix Cache 与 KV Cache 多级复用:前缀缓存的命中率模型、跨请求复用、显存/内存/远端三级缓存的设计
- KV Cache 池化:跨实例共享的架构、一致性代价与失效边界
- 前缀感知路由与负载均衡:路由策略如何影响缓存命中率——这是一个容易被忽略的耦合
- encoder 与 LLM 的分离部署:多模态 prefill 极重,PD 分离收益比纯文本更大;encoder 输出缓存复用
- 编排层:Triton Inference Server、Dynamo、KServe、Ray Serve 与引擎层的接口契约

写作要求:
- 问题场景请写一个多轮对话服务:每轮都重算完整前缀,算力浪费七成
- PD 分离要给出明确的适用判断,不要含糊其辞
- 路由与缓存的耦合关系是本章的一个亮点,要讲透

视觉规划(4 张):
1. PD 分离架构图:Prefill 池、Decode 池、KV 传输通道,标注传输量与带宽需求。用 flowchart LR + subgraph。
2. KV Cache 三级缓存图:显存、内存、远端三级的容量、延迟、命中率关系。用 flowchart TB。
3. 前缀感知路由示意图:同前缀请求被路由到同一实例 vs 随机路由的命中率差异对比。用 flowchart LR,失效路径用红色。
4. 决策工件:"我的负载该不该做 PD 分离、该做几级缓存"。
```

---

### 第 25 章 弹性与多模型

```
撰写第 25 章《弹性与多模型》。

必须覆盖:
- 冷启动:权重加载、图编译、预热三段的时间构成与各自的优化手段;冷启动时间如何决定弹性策略的可行性
- 扩缩容:指标选择(为什么 CPU 利用率类指标在这里没用)、滞后问题、预热池的成本模型
- 多模型共存与 LoRA 多租户:原理见 §3.6,本章讲部署实现——共享基座、动态加载、切换开销
- 模型热更新与灰度切换:如何做到不断流

写作要求:
- 问题场景请写一个突发流量:扩容指令下发后五分钟才有新实例可用,业务已经超时
- 冷启动的时间拆解要具体,让读者知道该优化哪一段
- "大数据对照"讲在线服务弹性伸缩经验的适用与不适用:模型服务的冷启动量级完全不同

视觉规划(3 张):
1. 冷启动时间构成图:各阶段耗时占比与可优化空间。用 gantt。
2. LoRA 多租户部署架构图:共享基座 + 多适配器的显存布局与请求路由。用 flowchart LR + subgraph。
3. 决策工件:"我的流量特征该用哪种弹性策略"。
```

---

### 第 26 章 特殊负载

```
撰写第 26 章《特殊负载》。

本章的作用是划清边界:告诉读者什么时候不能套用文本 LLM 的经验。

必须覆盖:
- 长上下文:显存墙、注意力复杂度增长、分块与卸载策略;上下文长度如何成为容量规划的主导变量
- Diffusion:迭代去噪的计算模式;compute-bound 特性;显存随分辨率 × 帧数增长;批处理策略与自回归完全相反——这是本章最重要的对照
- 实时语音:端到端延迟预算的逐段拆解;流式处理的特殊要求
- 三类负载与文本 LLM 在架构决策上的分歧点汇总

写作要求:
- 问题场景请写一个团队把文本 LLM 的部署经验直接套到视频生成上的失败
- Diffusion 与自回归的批处理策略对立是本章的核心洞察,要讲透
- 本章可以相对紧凑,重在指出差异而非穷尽细节

视觉规划(3 张):
1. 三类负载与文本 LLM 的资源画像对比图:在算力/显存/带宽/延迟四个维度上的差异。用 flowchart LR。
2. Diffusion 与自回归的批处理模式对比:两种模式下的计算与显存占用时间轴。用 gantt。
3. 决策工件:"我的负载类型该套用哪套部署经验"。
```

---

## 第六部分 上层平台

### 第 27 章 模型网关

```
撰写第 27 章《模型网关》。

必须覆盖:
- 统一 API 层与协议适配:多供应商协议差异的抹平代价
- 限流、配额、计费的 token 级实现(token 口径见 §3.3):为什么按请求数限流在这里失效
- 多供应商路由、故障转移、降级策略:路由决策的输入变量与优先级
- 缓存与去重:语义缓存的收益与风险

写作要求:
- 问题场景请写一次供应商故障:没有故障转移能力,整个产品线中断四小时
- token 级计费与限流是本章的技术核心,要给出可实现的设计
- "大数据对照"讲 API 网关经验的直接迁移与新增的复杂度

视觉规划(3 张):
1. 网关架构分层图:协议适配、路由、限流计费、缓存四层职责。用 flowchart TB + subgraph。
2. 一次请求的网关内部路径:含故障转移分支的完整决策链路。用 flowchart LR,降级路径用红色。
3. 决策工件:"我的网关该做到哪一层"。
```

---

### 第 28 章 Agent 运行时基础设施

```
撰写第 28 章《Agent 运行时基础设施》。

【边界】本章只讲基础设施接口,Agent 的架构设计不在本书范围。遇到架构设计问题请明确引导读者去看专门讨论 Agent 的资料,不要展开。

必须覆盖:
- 沙箱与代码执行:隔离级别选择(容器/微 VM/WASM)、启动延迟、资源限制;安全与性能的取舍
- 工具调用的基础设施:超时、重试、并发控制;工具调用失败对整体延迟的放大效应
- 会话状态与上下文存储:状态规模估算、存储选型、过期策略
- RAG 的基础设施视角(引用第 15 章,不重述索引细节)

写作要求:
- 问题场景请写沙箱冷启动成为 Agent 延迟主因
- 本章相对紧凑,重在给出资源与隔离方案的设计依据
- "大数据对照"讲多租户计算沙箱的老经验在这里的复用

视觉规划(3 张):
1. 沙箱隔离级别对比图:三种方案在隔离强度、启动延迟、资源开销上的位置。用 flowchart LR。
2. 一次 Agent 任务的资源占用时序:模型推理、工具调用、沙箱执行三类资源的交替占用。用 gantt。
3. 决策工件:"我的 Agent 负载该选哪种沙箱方案"。
```

---

### 第 29 章 可观测性与评测流水线

```
撰写第 29 章《可观测性与评测流水线》。

必须覆盖:
29.1 可观测性:
- LLM 专属指标体系:TTFT、TPOT、吞吐、并发、MFU、缓存命中率各自的定义、采集方式与常见误用
- 指标/日志/Trace 在 AI 场景的特殊性:一次请求的 trace 该记录到什么粒度
- 训练侧可观测性(引用 §5.4 profiling 方法论)
- 告警设计与根因定位路径

29.2 评测工具链:
- 精度评测与性能评测的分工(概念见 §3.7):两套体系、两套指标、两套基础设施
- 精度评测工具的定位与差异(OpenCompass、EvalScope 等)
- AISBench 兼具精度与性能 benchmark 能力的定位
- Infra 侧真正关心的三件事:评测任务本身的调度成本(它也是个吃卡的批任务)、结果的可复现性、以及如何接进 CI 做回归
- 评测流水线:回归评测、A/B、线上质量监控
- 谁来跑评测:平台提供能力还是业务方自持

写作要求:
- 问题场景请写一次量化上线事故:性能指标全绿,但模型质量掉了,没有任何监控发现
- 评测作为"吃卡的批任务"这个视角是本章的差异化,要讲透
- "大数据对照"讲数据质量监控体系的迁移

视觉规划(4 张):
1. LLM 指标体系全景图:按训练侧/推理侧/业务侧三层组织,标注各指标的采集点。用 flowchart TB + subgraph。
2. 一次推理请求的指标采集点标注图:在请求链路上标出各指标的测量位置。用 sequenceDiagram。
3. 评测流水线接入 CI 的流程图:模型产出 → 精度评测 → 性能评测 → 卡点判定 → 上线。用 flowchart LR,卡点用红色。
4. 决策工件:"我的平台该建到哪一级可观测性"。
```

---

## 第七部分 工程化与治理

### 第 30 章 模型生命周期管理

```
撰写第 30 章《模型生命周期管理》。

必须覆盖:
- 模型注册、版本、血缘:血缘要追到数据集与代码版本
- 灰度、回滚、影子流量:模型灰度与普通服务灰度的差异——效果问题不像故障那样立即可见
- 制品管理与权重分发(接第 25 章冷启动):权重分发是被低估的一环
- 评测与上线的准入卡点(工具链引用 §29.2)
- 与数据飞轮的闭环接口(接第 13 章)

写作要求:
- 问题场景请写一次回滚:发现模型质量下降时,已经无法确定当前线上是哪个版本、用什么数据训的
- 本章相对紧凑,重在流程设计而非工具介绍
- "大数据对照"讲数据血缘与模型血缘的异同

视觉规划(3 张):
1. 模型全生命周期图:从数据到训练到评测到上线到回流的完整闭环,标注各环节的制品与卡点。用 flowchart LR。
2. 血缘关系图:模型版本关联的数据集版本、代码版本、超参配置、基座模型。用 flowchart TB。
3. 决策工件:"我的团队规模该建到哪一级生命周期管理"。
```

---

### 第 31 章 容量、成本与 SLO

```
撰写第 31 章《容量、成本与 SLO》。

本章是全书的收口,把前面所有技术决策折算成钱和数字。

必须覆盖:
31.1 容量与压测:
- 容量模型:从业务指标反推卡数(引用 §6.2 换算公式)
- 采购粒度的变化:超节点使最小采购单元从"台"变为"域/柜",容量规划的台阶变粗
- 压测方法论:负载建模、长尾、饱和点的确定
- SLO 定义与容量水位

31.2 机房、功率与散热(§4.1.4 埋的伏笔在此展开):
- 功耗密度是根因:单卡功耗翻倍后,单机柜功率从十几千瓦跃升到上百千瓦,风冷在某个密度点直接失效
- 风冷/冷板式液冷/浸没式液冷的适用密度区间与改造成本
- 超节点必然伴随液冷:高密度部署是超节点的物理前提
- 降频与热节流:压测跑得好、跑一小时掉性能——很多"性能不稳定"问题的真凶;标称算力与可持续算力的差距由散热决定
- PUE 与电费如何进入 TCO 模型
- 老机房改造的现实约束:承重、配电、水路,及其对硬件选型的反向锁定

31.3 成本:
- TCO 建模:硬件、电力、机房改造、运维人力
- 自建 vs 云 vs 混合的决策框架
- 国产卡在 TCO 模型里的正确算法(引用 §12.5 对标口径与 §12.7 生态折算)
- 成本归因与优化杠杆排序

写作要求:
- 问题场景请写一次容量规划失败:按算力买够了卡,但机房供电不足,一半机柜上不了电
- 全书的收口章,要把前面各章的技术决策统一折算到成本坐标上
- 热节流那一段要写得足够醒目,这是很多人没意识到的问题

视觉规划(4 张):
1. 从业务指标到卡数的完整推导链:QPS/SLO → token 吞吐 → 单卡吞吐 → 实例数 → 卡数 → 机柜数 → 功率需求。用 flowchart LR,这是全书的收口主图。
2. 散热方案与功率密度的适配区间图:三种方案的适用密度带与改造成本。用 flowchart LR 或 xychart-beta。
3. TCO 构成图:硬件、电力、机房、人力、生态折算五块的相对占比。用 flowchart TB。
4. 决策工件:"我该自建还是上云,该买哪类硬件"。全书最后一棵决策树,要能综合前面各章的结论。
```

---

## 附录写作 Prompt(统一)

```
撰写附录 [A/B/C/D/E/F]。

附录的定位与正文完全不同:正文写机制与方法论,附录承载会过时的具体数据与可复用的工具。

统一要求:
- 附录 A、C 必须在页首标注"数据截至 XXXX 年 X 月",并给出在线勘误地址
- 附录 B 的公式必须与正文各章的符号完全一致,不得出现正文用 N、附录用 P 这类不一致
- 附录 E 必须是可直接打印使用的清单/打分表形式,不是叙述性文字
- 附录 F 要明确说明"本书不覆盖这些内容是有意为之",并按读者目的分类推荐

各附录内容见大纲文件对应章节。

附录不需要 mermaid 图,以表格为主。附录 B 可以有一张"公式依赖关系图",说明哪个公式的输入来自哪个公式。
```

---

# 第三层:审查 Prompt(v3 新增)

> 顺序强制:**先事实与口径审查,后语言与结构审查**。不允许在事实审查前做语言润色——润色会掩盖口径问题。

## 事实与口径审查 Prompt

```
你是《AI Infra》的事实审查员。对给定章节草稿逐段检查,只关注事实与口径,不改文风:

1. 每个具体数字:属于官方规格 / 公开实测 / 作者实测 / 推导估算 / 示意数据中的哪一类?是否已登记 claim 或标注示意?无法归类的列入问题清单。
2. 口径混用检查(逐项过):稠密 vs 稀疏算力;单向 vs 双向带宽;峰值 vs 持续性能;标称 vs 实际可用容量;已发布 / GA vs 路线图 / 送样;每 token vs 每请求;TTFT 的测量起点。
3. 案例元数据块是否存在且与正文一致;合成案例是否含声明行。
4. 强立场是否绑定依据、边界、时间三件套。
5. 跨章引用的编号与单点定义表是否一致。
6. 高时效内容(项目状态、版本、价格)是否只出现在附录/claim 而未固化进正文机制描述。

输出:问题清单(文件、位置、问题类型、建议处理),不直接改稿。
```

## 语言与结构审查 Prompt

```
你是《AI Infra》的结构与语言审查员。前提:该章已通过事实审查。检查并修复:

1. 章节围绕一个工程问题推进,内部七项能力有覆盖但不暴露为固定标题序列;结尾有可验证交付物。
2. 段落之间有推进关系,不是并列知识点清单;收束落在工程后果、验证动作、适用边界或未决问题。
3. 术语首现给英文原文,之后统一;无空转开头;无版本号与 API 签名混入正文。
4. Mermaid 规范:init 块、语义配色、红色仅瓶颈/故障、边标签、节点数、图注格式与编号。
5. 语言修复不得改变任何数字、口径与立场表述——发现事实问题退回事实审查,不得顺手改。
```

# 新增主题覆盖对照(v3,对应计划 Task Group 11)

> 以下主题按计划 §17 分批补入现有章节;**每个主题动笔前必须先建 Research Pack**。落点对照:

| 主题 | 目标章节 |
|---|---|
| 可信平面与模型供应链 | 2、13、19、27、28、30、31 + 附录 C |
| Kubernetes DRA | 8、9 + 附录 C |
| JobSet / Kueue / MultiKueue | 9、19、20 + 附录 C |
| 模型感知推理路由(InferencePool/EPP) | 24、25、27 + 附录 C |
| Agent Runtime 2.0 与 MCP | 27、28、29、30 + 附录 C |
| OpenTelemetry GenAI 可观测性 | 27–29 + 附录 C |
| Agentic RL 基础设施 | 21、20、29–30 + 附录 C |
| 能源、碳强度与环境指标 | 11、29、31 + 附录 A/B |

---

# 使用建议

0. **v3 流程**:Research Pack → 草稿 → Claim/来源登记 → 事实与口径审查 → 语言与结构审查 → CI(`npm run docs:check:evidence` + `docs:check:prompts`)。
1. **写作顺序建议**:先用第 1、7、22 章分别验证认知迁移、机制定量和运行时内存三类写法,评审后再批量推进。
2. **图表统一性检查**:全书约 100 张图,建议写完后单独做一轮图表审查,重点检查配色语义是否一致、红色是否只用于瓶颈和故障、图注是否都写成了结论而非复述。
3. **单点定义检查**:公共前置 prompt 里的定义表已有 20 条,建议单独维护为一个文件,并写一个校验脚本扫描正文中的"定义式表述"。
4. **第 6 段的硬性检查**:"大数据对照"最容易在赶稿时被省掉,建议在校验脚本里设为必检项。
