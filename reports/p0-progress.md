# P0 章节迁移进度

> 三章试点完成后按单章迁移。每章必须通过审计、链接、视觉再生成和站点构建，才进入下一章。

## 第 4 章：硬件第一性原理、架构范式与数值精度

状态：已迁移，待人工技术与可读性抽查。

| 指标 | 基线 | 当前 |
|---|---:|---:|
| 粗体占比 | 7.16% | 6.47% |
| 相似段落三连 | 2 | 1 |
| 数字化案例缺元数据 | 1 | 0 |
| 图片/图表 | 4 | 5 |
| Mermaid | 1 | 1 |
| 非 Mermaid | 3 | 4 |

完成内容：

- 用“如何判断哪个规格数字限制自己的负载”统一章节主问题；
- 将无来源的公司采购故事改为 `capacity-example`，规格全部使用归一化示意指数；
- 新增加速卡四个关键数字对照图，登记 CSV、生成脚本、alt、结论型图注和许可；
- 将算力—带宽代际关系改为按具体代际和精度核验，不再外推为永久规律；
- 将 HBM 供应表述改为带采购时间、容量规格和供应商边界的供应链判断；
- 保留 §4.1–§4.4 及其下级标题，确保其他章节和附录的锚点兼容。

人工抽查重点：

- Roofline 公式中的 FLOPs、Bytes 与峰值精度口径是否一致；
- 精度谱系表的“相对算力倍率”是否会被误读为跨硬件承诺；
- 统一池、双池、专用 ASIC 三种路线的失效边界是否覆盖实际采购场景；
- 新图中的归一化指数是否足够醒目地表明“不代表真实产品”。

## 第 6 章：模型结构的定量分析

状态：已迁移，待人工公式与容量口径抽查。

| 指标 | 基线 | 当前 |
|---|---:|---:|
| 粗体占比 | 7.09% | 6.38% |
| 相似段落三连 | 4 | 0 |
| 数字化案例缺元数据 | 1 | 0 |
| 图片/图表 | 3 | 5 |
| Mermaid | 2 | 2 |
| 非 Mermaid | 1 | 3 |

完成内容：

- 用“结构输入 → 显存线/算力线 → 待实测系数”统一章节主问题；
- 将虚构团队失误改为 `capacity-example`，分开标注模型输入、系统假设和理想工期；
- 新增容量工作表与 70B 单卡训练显存瀑布，登记 CSV、生成脚本、alt、图注与许可；
- 修正 70B 算例中“声明 ZeRO-1、实际按未启用 ZeRO 计算 35 GB”的口径冲突；
- 将固定 MFU 区间、跨模型多模态 token 档位和静态估算精度改为待实测假设；
- 保留 §6.1–§6.5 标题，确保跨章锚点兼容。

人工抽查重点：

- 选择性重计算激活与 PP in-flight micro-batch 的 5 GB 示例是否足够醒目标为待测；
- GB/GiB、KB/KiB 与硬件标称容量在公式和图中的口径是否会被混用；
- MoE All2All、长上下文注意力修正和多模态编码器算力是否覆盖目标模型变体；
- 理想计算时长与含可用率、checkpoint 停顿的墙钟工期是否区分清楚。

下一章：第 17 章显存治理与训练内存优化。

## 第 17 章：显存与训练侧精度机制

状态：已迁移，待人工运行时与数值机制抽查。

| 指标 | 基线 | 当前 |
|---|---:|---:|
| 粗体占比 | 9.92% | 3.29% |
| 相似段落三连 | 1 | 0 |
| 数字化案例缺元数据 | 0 | 0 |
| 图片/图表 | 3 | 4 |
| Mermaid | 2 | 2 |
| 非 Mermaid | 1 | 2 |

完成内容：

- 用“OOM 证据单”和“数值异常证据单”替代固定的链路、依赖和虚构事故骨架；
- 为第 3000 步 NaN 合成案例补齐数据性质、缺失证据和可复现边界；
- 新增 ZeRO 状态分片对照与重计算显存—计算交换曲线，登记 CSV、生成脚本、alt、图注和许可；
- 修正“ZeRO-3 与 PP 机制互斥”的错误，改为核算运行时组合、重复聚合与 trace；
- 移除 `reserved−allocated` 固定 GB 阈值，将碎片判断改为请求块、inactive split、segment 布局与时间线的联合证据；
- 收紧 FP8、BF16、offload 和重计算的固定收益或绝对失效结论；
- 同步修正计算器、附录 A/B 与 OOM runbook 的章节链接和归因口径。

人工抽查重点：

- ZeRO-1/2/3 的静态字节公式是否与目标运行时的梯度、参数释放时机一致；
- PP+完全分片在目标框架中的 wrap、预取和 micro-batch 聚合次数是否被准确描述；
- BF16/FP8 scaling、首个异常张量与硬件低精度路径的分诊是否存在遗漏；
- OOM EvidencePanel 能否让值班人员在改变配置前保存足够现场。

## 第 11 章：多租户与利用率治理

状态：已迁移，待人工指标与治理边界抽查。

| 指标 | 基线 | 当前 |
|---|---:|---:|
| 粗体占比 | 10.76% | 0.00% |
| 相似段落三连 | 0 | 0 |
| 数字化案例缺元数据 | 1 | 0 |
| 图片/图表 | 3 | 5 |
| Mermaid | 3 | 2 |
| 非 Mermaid | 0 | 3 |

完成内容：

- 用“占用率不等于有效产出”统一章节主问题，并补齐作业生命周期事件、训练窗口与窗口内 MFU 三层边界；
- 将原先重复相乘的 SM 活跃度移出核算公式，合成案例统一为 `85% × 62% × 40% = 21.08%`；
- 新增有效产出漏斗、归因实验矩阵和利用率—排队—恢复风险曲线，登记 CSV、生成脚本、alt、图注与许可；
- 按 NVIDIA 官方定义区分 GPU utilization、SM Activity 与 SM Occupancy，移除跨集群固定 MFU 和告警阈值；
- 将自动定责改为作业事件、profiler 时间线和单变量实验联合举证，并为回收、借用与准入增加影子策略和恢复护栏；
- 移除 `TDP × utilization` 能耗估算、固定 ROI 倍数和大数据弹性成本量级断言，改为功率积分、系统边界与 SLO 联合决策；
- 保留 §11.4.1～§11.7 标题，确保跨章锚点兼容。

人工抽查重点：

- Training-window share 的首末 step、失败重启和暂停状态是否能由目标调度器事件稳定复原；
- MFU 分子对 MoE、长上下文、重计算与非 Transformer 模型的处理是否与训练框架一致；
- 自动回收的影子模式、合法低活动阶段声明和一键恢复能否覆盖强化学习与交互式开发；
- 分配占用率目标是否与目标集群的排队分位数、故障重排和恢复 SLO 同时复核。

## 第 12 章：国产算力平台与异构迁移

状态：已迁移，待人工后端集成与成本模型抽查。

| 指标 | 基线 | 当前 |
|---|---:|---:|
| 粗体占比 | 6.88% | 0.00% |
| 相似段落三连 | 1 | 0 |
| 数字化案例缺元数据 | 1 | 0 |
| 图片/图表 | 4 | 5 |
| Mermaid | 3 | 1 |
| 非 Mermaid | 1 | 4 |

完成内容：

- 用“统一 API 只覆盖适配面的一角”替代无来源的九个月真实项目叙事，并为 32B 合成评估补齐数据性质与复现入口；
- 将迁移对象冻结为设备、固件驱动、编译器、框架后端、通信库、模型与负载版本组合，不再给厂商做长期成熟度排名；
- 按功能、数值、性能与运行恢复证据重写三道坎，显式区分原生 kernel、组合实现、图编译、CPU fallback 和缺失路径；
- 新增五层适配面、规模效率曲线、一次性与持续成本账，登记 CSV、生成脚本、alt、图注与许可；
- 修正“单卡跑分没有意义”和“MFU 是跨平台诚实分母”的绝对结论：局部跑分用于诊断，跨平台决策使用质量门禁后的端到端产出、规模效率和单位全成本产出；
- 移除固定精度阈值、算子人月、MFU 比例、团队人数与生态成本区间，改为目标负载基线、单变量实验和情景估算；
- 同步修正附录 D/E、异构参考架构和第 31 章中固定两周、固定门槛、`MFU × 规模效率` 与 30～80 人月常态断言；
- 保留 §12.1～§12.7 及全书唯一定义处标题，确保跨章锚点兼容。

人工抽查重点：

- 目标框架后端对 AMP、分布式、随机数、序列化和 fallback 的支持状态是否与冻结版本一致；
- 数值基线能否覆盖随机训练波动、端到端质量和目标业务的合规验证要求；
- 规模效率曲线是否跨过生产互联域，并对双方使用等量调优预算；
- 一次性与年度持续成本的摊销年限、机会成本和退出风险是否使用统一货币与统计期。

## 第 14 章：存储与 Checkpoint I/O

状态：已迁移，待人工存储语义与恢复协议抽查。

| 指标 | 基线 | 当前 |
|---|---:|---:|
| 粗体占比 | 8.62% | 0.00% |
| 相似段落三连 | 0 | 0 |
| 数字化案例缺元数据 | 1 | 0 |
| 图片/图表 | 3 | 5 |
| Mermaid | 2 | 1 |
| 非 Mermaid | 1 | 4 |

完成内容：

- 将无来源的 2048 卡事故改为带元数据的合成证据链，并把保存窗口拆成 staging、排队、分片写入、尾部同步与提交；
- 修正“参数量即可精确得到 checkpoint 大小”，改为实际 state dict 张量、元数据、格式与各 rank 分布核算；`16N` 只保留为声明状态组成的容量示例；
- 新增 checkpoint 关键路径、同相突发争用曲线和恢复语义矩阵，登记 CSV、生成脚本、alt、图注与许可；
- 将 `S/min(B)` 限定为数据段物理下界，元数据、队列、最慢 rank、commit 与 verify 使用独立计时；
- 补齐临时 generation、manifest、校验和、commit marker、restore 与垃圾回收顺序，区分写 API 返回、上传完成和可恢复版本；
- 移除固定存储折扣、元数据秒数、1% 红线和按 64/512 卡分档，改用 RPO/RTO、故障域、共享 SLO、暂存容量和恢复来源选型；
- 为 Young 一阶近似补齐同步周期保存与故障模型假设，并同步修正 checkpoint 计算器、Lab 05、Runbook 05 与两份参考架构；
- 保留 §14.1～§14.7、§14.4.3 和 §14.5.1 等跨章锚点。

人工抽查重点：

- 目标框架的 state dict 是否遗漏梯度、数据游标、scaler、RNG 或自定义状态；
- 目标存储的 commit/latest 语义、校验和与失败 generation 清理是否真的原子或可恢复；
- 异步 staging 是否会与 optimizer 更新竞争，并在 CPU、内存、网络或暂存盘上形成新瓶颈；
- 故障相关性、检测延迟、恢复时间与业务 RPO 是否使 Young/Daly 初筛失效。

## 第 16 章：并行策略

状态：已迁移，待人工框架集成与目标拓扑短跑抽查。

| 指标 | 基线 | 当前 |
|---|---:|---:|
| 粗体占比 | 9.91% | 0.00% |
| 相似段落三连 | 0 | 0 |
| 数字化案例缺元数据 | 0 | 0 |
| 图片/图表 | 4 | 6 |
| Mermaid | 2 | 1 |
| 非 Mermaid | 2 | 5 |

完成内容：

- 将“按五种并行逐一百科介绍”改为容量、形状、批量、拓扑、训练语义与运维六类约束先行，再生成和过滤候选网格；
- 把 256 卡“三倍吞吐”叙事改为有元数据的容量示例，只保留 TP16 必然跨 8 卡高速域这一可验证事实，不再虚构 MFU 与归因结论；
- 将固定 TP/EP/CP collective 次数与通信量改成按目标计算图或 trace 求和的事件账，明确区分 payload、链路模型和端到端 step time；
- 把流水线气泡公式限定为等时 stage、GPipe 式调度且忽略通信的理想下界，不再把它套到 1F1B 与 interleaved；
- 新增并行通信证据矩阵、理想气泡曲线和候选可行域，登记 CSV、生成脚本、alt、图注与许可；
- 将“TP/EP 不跨域”改成优先放置规则，跨域候选需提交实际进程组、通信暴露时间和受控短跑证据；
- 按官方文档版本化 PyTorch DeviceMesh/pipelining 与 Megatron Core 能力，不做框架永久排名；
- 同步修正 128 卡和千卡参考架构中的跨域铁律及“估算后不压 MFU”结论，并保留 §16.1～§16.7 与 §16.4.1～§16.4.8 锚点。

人工抽查重点：

- 目标框架实际生成的 DP/TP/PP/EP/CP 进程组、collective 次数和参数共享语义是否与工作表一致；
- 目标模型的 Q/KV 头、hidden/FFN、专家、层数与 microbatch 是否满足 kernel 和 schedule 的版本化整除规则；
- 流水线短跑是否记录逐 stage 计算、P2P 暴露、空闲率、激活峰值与数值一致性，而非只看平均吞吐；
- checkpoint 是否能从该分片网格保存、重映射恢复并续训，超节点故障后的 spare 与重启粒度是否可接受。

## 第 20 章：稳定性与容错

状态：已迁移，待人工事件去重、恢复演练与 SLO 抽查。

| 指标 | 基线 | 当前 |
|---|---:|---:|
| 粗体占比 | 5.06% | 0.00% |
| 相似段落三连 | 3 | 0 |
| 数字化案例缺元数据 | 0 | 0 |
| 图片/图表 | 5 | 6 |
| Mermaid | 3 | 1 |
| 非 Mermaid | 2 | 5 |

完成内容：

- 将“两周十七次重启”改为有元数据的合成事件账，明确重启次数、根因事件和部件故障率不能直接互换；
- 用 Poisson 风险曲线表达期望中断数与至少一次中断概率，并补齐独立、指数失效和任一事件中断任务的适用假设；
- 将固定单卡/节点/链路故障率改成本地任务影响事件去重与低/中/高情景，不再用卡数直接除 MTBF；
- 将 ETTR 改为本书定义的 productive-share 互斥时间账，失败回滚或质量拒绝的 step 不进入分子；
- 把 Young/Daly 公式限定为周期阻塞保存的一阶模型，新增 checkpoint 写入—回滚成本曲线并分离检测、排队和恢复成本；
- 新增任务中断概率、checkpoint 成本曲线和恢复时间线，登记 CSV、生成脚本、alt、图注与许可；
- 移除慢节点 3～5%、热备 2～5%、千卡 90% 等通用阈值，改为同角色健康分布、关键路径证据和止损成本比较；
- 修正“同布局必然 bitwise 一致”，采用状态完整、受控重放和统计/质量等价三层恢复验收；
- 为跨集群恢复补充 generation/lease fencing 和唯一写者语义，并同步修正三份参考架构与慢节点 Runbook；
- 保留 §20.1～§20.7、§20.4.1～§20.4.8 及“跨集群训练的故障语义”锚点。

人工抽查重点：

- 任务事件是否按共同时间窗与故障域去重，监控告警、rank 报错和部件更换是否错误地重复计数；
- checkpoint 的 `C` 是否真的是训练 pause，任务影响事件间隔 `M` 是否排除了不导致回滚的维护与告警；
- 恢复完成是否以连续提交质量门禁通过的 step 为准，并覆盖 optimizer、RNG、数据游标和并行网格；
- 超节点的互联域、故障域、重调度域与 checkpoint 副本故障域是否经过真实注入验证；
- 弹性缩容是否验证新网格、全局 batch/学习率、状态重映射和再次扩容成本。

## 第 23 章：推理优化——计算类加速

状态：已迁移，待人工目标引擎、质量门禁与组合负载抽查。

| 指标 | 基线 | 当前 |
|---|---:|---:|
| 粗体占比 | 9.15% | 0.00% |
| 相似段落三连 | 2 | 0 |
| 数字化案例缺元数据 | 0 | 0 |
| 图片/图表 | 6 | 6 |
| Mermaid | 1 | 1 |
| 非 Mermaid | 5 | 5 |

完成内容：

- 将量化、融合、图模式和投机推理的并列百科改为“冻结负载—关键路径归因—单变量候选—质量门禁—端到端 Pareto”；
- 把无来源的 32B 生产事故改为有元数据的合成实验设计，明确不同分母的单项加速比不可相乘；
- 将 `t_launch+t_mem+t_compute+t_post` 改为来源清单，墙钟归因使用互斥区间并以 Amdahl 公式约束局部优化上界；
- 移除 W4/W8、FlashAttention、融合、图模式的固定加速区间和通用 batch/命中阈值，改为目标 kernel、shape、HBM、occupancy 与 fallback 证据；
- 将量化权重 payload 下界与 runtime packed bytes 分开，并把校准集、质量切片和引擎原生 kernel 设为双门禁；
- 修正投机解码盈亏模型，将目标验证成本 `v` 显式加入分母，删除与原公式不一致的 0.5～0.6 接受率红线；
- 新增 kernel 时间线、质量—吞吐—尾延迟 Pareto 和双验证成本投机收益曲线，登记 CSV、生成脚本、alt、图注与许可；
- 把“量化—融合—图模式安全主干”改为条件冲突矩阵，任何组合都作为新候选重新跑完整实验；
- 同步修正第 25 章图编译固定冷启动时长、90% 缓存收益和免费裁剪断言；
- 保留 §23.1～§23.5 及全部二级标题锚点。

人工抽查重点：

- 目标引擎是否真正命中声明的量化、attention、融合和 graph kernel，而非 fallback 或独立 quant/dequant；
- 质量评测是否覆盖线上领域、长度、采样、安全和结构化输出，并与校准集隔离；
- graph hit/update/fallback、bucket padding、静态显存和 KV 并发是否在相同到达过程中联合测量；
- 投机解码是否用实际接受长度分布、草稿成本 `c`、验证成本 `v`、batch padding 和调度结果复算；
- 组合候选是否重新测量端到端 TTFT、TPOT、有效吞吐、成功率、显存、功耗和单位有效 token 成本。

下一章：第 24 章服务化架构与显存类加速。
